# Ultracite Code Standards

This project uses **Ultracite**, a zero-config preset that enforces strict code quality standards through automated formatting and linting.

## Quick Reference

- **Format code**: `bun x ultracite fix`
- **Check for issues**: `bun x ultracite check`
- **Diagnose setup**: `bun x ultracite doctor`

Biome (the underlying engine) provides robust linting and formatting. Most issues are automatically fixable.

---

## Core Principles

Write code that is **accessible, performant, type-safe, and maintainable**. Focus on clarity and explicit intent over brevity.

### Type Safety & Explicitness

- Use explicit types for function parameters and return values when they enhance clarity
- Prefer `unknown` over `any` when the type is genuinely unknown
- Use const assertions (`as const`) for immutable values and literal types
- Leverage TypeScript's type narrowing instead of type assertions
- Use meaningful variable names instead of magic numbers - extract constants with descriptive names

### Modern JavaScript/TypeScript

- Use arrow functions for callbacks and short functions
- Prefer `for...of` loops over `.forEach()` and indexed `for` loops
- Use optional chaining (`?.`) and nullish coalescing (`??`) for safer property access
- Prefer template literals over string concatenation
- Use destructuring for object and array assignments
- Use `const` by default, `let` only when reassignment is needed, never `var`

### Async & Promises

- Always `await` promises in async functions - don't forget to use the return value
- Use `async/await` syntax instead of promise chains for better readability
- Handle errors appropriately in async code with try-catch blocks
- Don't use async functions as Promise executors

### React & JSX

- Use function components over class components
- Call hooks at the top level only, never conditionally
- Specify all dependencies in hook dependency arrays correctly
- Use the `key` prop for elements in iterables (prefer unique IDs over array indices)
- Nest children between opening and closing tags instead of passing as props
- Don't define components inside other components
- Use semantic HTML and ARIA attributes for accessibility:
  - Provide meaningful alt text for images
  - Use proper heading hierarchy
  - Add labels for form inputs
  - Include keyboard event handlers alongside mouse events
  - Use semantic elements (`<button>`, `<nav>`, etc.) instead of divs with roles

### Error Handling & Debugging

- Remove `console.log`, `debugger`, and `alert` statements from production code
- Throw `Error` objects with descriptive messages, not strings or other values
- Use `try-catch` blocks meaningfully - don't catch errors just to rethrow them
- Prefer early returns over nested conditionals for error cases

### Code Organization

- Keep functions focused and under reasonable cognitive complexity limits
- Extract complex conditions into well-named boolean variables
- Use early returns to reduce nesting
- Prefer simple conditionals over nested ternary operators
- Group related code together and separate concerns

### Security

- Add `rel="noopener"` when using `target="_blank"` on links
- Avoid `dangerouslySetInnerHTML` unless absolutely necessary
- Don't use `eval()` or assign directly to `document.cookie`
- Validate and sanitize user input

### Performance

- Avoid spread syntax in accumulators within loops
- Use top-level regex literals instead of creating them in loops
- Prefer specific imports over namespace imports
- Avoid barrel files (index files that re-export everything)
- Use proper image components (e.g., Next.js `<Image>`) over `<img>` tags

### Framework-Specific Guidance

**Next.js:**

- Use Next.js `<Image>` component for images
- Use `next/head` or App Router metadata API for head elements
- Use Server Components for async data fetching instead of async Client Components

**React 19+:**

- Use ref as a prop instead of `React.forwardRef`

**Solid/Svelte/Vue/Qwik:**

- Use `class` and `for` attributes (not `className` or `htmlFor`)

---

## Testing

- Write assertions inside `it()` or `test()` blocks
- Avoid done callbacks in async tests - use async/await instead
- Don't use `.only` or `.skip` in committed code
- Keep test suites reasonably flat - avoid excessive `describe` nesting

## When Biome Can't Help

Biome's linter will catch most issues automatically. Focus your attention on:

1. **Business logic correctness** - Biome can't validate your algorithms
2. **Meaningful naming** - Use descriptive names for functions, variables, and types
3. **Architecture decisions** - Component structure, data flow, and API design
4. **Edge cases** - Handle boundary conditions and error states
5. **User experience** - Accessibility, performance, and usability considerations
6. **Documentation** - Add comments for complex logic, but prefer self-documenting code

---

Most formatting and common issues are automatically fixed by Biome. Run `bun x ultracite fix` before committing to ensure compliance.

---

## Heimdallone Project Context

Heimdallone v2 is a multi-tenant HRMS/payroll/workforce platform.

### Stack
- Bun + Turborepo monorepo
- TanStack Start + React (frontend)
- Hono + oRPC (API server)
- Better Auth + Organization/Admin plugins (auth/RBAC, 9 tenant roles)
- Drizzle ORM + PostgreSQL (central Postgres)
- Tailwind CSS v4 + Heimdallone design handoff CSS (dark-first, gold-accent)
- shadcn/ui with `base-lyra` style (@base-ui/react, NOT Radix)

### Implementation Pattern
Each module follows: **A** (spec) → **B** (schema+seed) → **C** (API) → **D** (UI) → **E** (QA/RBAC).

### Current Status (2026-06-02)
- **QA hardening pass (2026-06-04, between 14B and 14C):** repo-wide static-audit follow-up — nav, gates, roadmap, placeholders. Closed the `isNavItemVisible` blanket-`return true` leak to `project_manager` (explicit allow-set); marked Compliance / Clients / Countries & Tax / Documents as **Preview** (sidebar pill + `<PreviewBanner>`, routes kept reachable, Compliance stats relabelled sample/demo + Export disabled); de-spoofed topbar sync ("Demo sync status"); `/dashboard`→`/app` redirects; web tsc **25→7**. **Added `.github/workflows/ci.yml`** (install + check-types + build + audit:permissions blocking; lint + web typecheck informational). Bank finding 8: no plaintext leak at API (masked read, HR/payroll-only full); at-rest plaintext documented as separate hardening item. **Documented baselines (not regressions):** web tsc 7 (legacy docs/settings/employees/login) — `apps/web` deliberately NOT in root `check-types` (its script is `typecheck`); lint 212; pre-commit hook lints whole staged file so debt-file fixes need a dedicated burn-down (lesson #83). **Canonical roadmap = `docs/architecture/modules/implementation-sequence.md` (Phase 13 Helpdesk ✅ COMPLETE; Phase 14 Projects ACTIVE: 14A spec ✅ → 14B DB ✅ → 14C API next).** The dated history below is retained but predates Phases 12–14.
- **Offboarding (Phase 10): ✅ COMPLETE** — 10A spec → 10B DB → 10C API (9 router groups; `assertCaseVisibleToCaller` manager-scope IDOR fix `907ebaa`; `cases.close` is the sole `isActive=false` writer; audit 62 pairs/9 routers) → 10D UI CP1–CP6 (overview/tabs, templates, cases list+detail, checklist actions, employee self-service, QA/RBAC pass `fa7210d`). Lint baseline 224/1.
- **Suite direction (user directive 2026-06-02):** Heimdallone = 5 product groups — **People & Payroll · Operations · Finance · CRM · Admin & Compliance** — one platform; future modules documented but kept hidden/queued in the sidebar until their phase. **Post-11 order:** 12 Assets → 13 Helpdesk/Requests → 14 Projects+Tasks/Timelines → 15 Performance/PMS → 16 Finance expansion → 17 CRM → 18 Analytics/dashboards → 19 Enterprise QA/a11y/security → 20 Production readiness. **CRM/Finance/Projects/Tasks NOT built yet.** CRM is furthest out — Phase **17A is research/spec-only** (doc `docs/architecture/crm-implementation-plan.md` already drafted: 8-table MVP, synthesises Odoo/ERPNext/TwentyCRM/SuiteCRM/EspoCRM/Vtiger/YetiForce/OroCRM/HubSpot/Salesforce/Zoho/Pipedrive/Attio/Dynamics + Horilla/OpenHRMS where HR∩CRM); CRM thesis = the cross-suite flow Lead→Customer→Deal→Project→Staff→Timesheets→Payroll cost→Invoice. Do not build CRM until People/Payroll + Assets + Helpdesk + Projects are stable unless told.
- **Leave Policy Engine (Phase 7I): ✅ COMPLETE (A/B/C/D/E).** 7I-E QA + soft payroll/leave warnings shipped: read-only `orgPolicies.health` + `unverifiedNotice` on balanceExplanation; warnings on company policies list, employee "why this balance", payroll run-preview review note (read-only, under confidence NOT blockers — **payroll engine/formulas untouched, 27/27 unchanged**), leave-request no-active-policy note. **Never blocks payroll, never changes pay.** Browser-verified all roles + direct-RPC RBAC (employee/auditor mutations 403; auditor read 200), 0 console errors, 6 screenshots docs/reviews/phase-7i-e/. Gates: check-types 3/3, build, lint 218/1/2, audit 86/12, verify 22/22, payroll-engine 27/27. **Next: return to Phase 12D Assets inventory UI.** 7I-D UI shipped (commit 960ea98): `features/leave/{policy-labels,leave-policy-tabs}` + routes `app/leave/policies/{index,company,template/$id,org/$id}` + `app/leave/balance`; template gallery w/ verification badges + source/review, template detail (rules + safe source link + adopt-snapshot-dialog), org policy detail (activate/archive/edit-rule/compare-to-baseline), employee "why this balance" (self-scoped). Payroll-treatment redacted non-finance; every non-verified rule shows verify-before-production notice. Browser-verified all roles, 0 console errors, 8 screenshots docs/reviews/phase-7i-d/. Gates: check-types 3/3, build, lint 218/1/2 (improved), audit 86/12, web tsc 0 touched-file errors, verify 22/22. Next 7I-E: surface a soft payroll warning for unverified/missing policy (engine path unchanged). Hardening of Phase 7 Leave. Spec `docs/architecture/leave-policy-engine-plan.md`; impl `docs/implementation/leave-policy-engine.md`. **Correction:** statutory leave ENTITLEMENT = Labour Act/Ministry of Labour/NIS; **GRA only governs payroll/PAYE/NIS treatment of leave pay** — never label entitlement a "GRA rule". **7I-A** researched + CITED Guyana: NIS maternity (13 wks @70%) + sickness (4th day, 26 wks @70%) **verified** (nis.org.gy); annual (Leave with Pay Act Cap.99:02) **needs_review** (labour.gov.gy PDF 403 — tenure bands unconfirmed); paternity/compassionate/unpaid **draft**. **No legal value without source metadata + verificationStatus; unverified ≠ legal certainty.** **7I-B** `schema/leave-policy.ts` 4 tables (leave_policy_template [org-nullable=system], leave_policy_rule [PER-RULE verificationStatus], organization_leave_policy [snapshot; sourceTemplateId SET NULL; partial-unique one active per (org,country)], organization_leave_policy_rule [snapshotted/editable, linkedLeaveTypeId]) + 7 enums, migration 0015 applied; seed-leave-policy.ts idempotent (GY 6 rules mixed verification + BB/TT/JM draft). **7I-C** `routers/leave-policy.ts` (`leavePolicy`): AC resource `leave_policy` (owner/admin/hr full; payroll/manager/auditor read; employee none); helpers canManageLeavePolicy/canViewLeavePolicy/canViewLeavePayrollTreatment. templates.list/getById (payroll-treatment redacted non-finance); orgPolicies.adoptTemplate (**transactional snapshot — system-template edits NEVER mutate an adopted policy**: copy-at-adoption + sourceRuleId SET NULL)/createCustom/copyFrom/updateRule/activate(CONFLICT one-per-country)/archive/compareToBaseline; balanceExplanation.forSelf/forEmployee (leave_request:read, self-scoped, never blocks payroll). verify-leave-policy-engine.ts **22/22** (snapshot invariant proven: system→99.00, adopted stayed 12.00). Gates check-types 3/3, lint 223/1/2, audit **86/12**. **Next: 7I-D UI** (template gallery + verification badges + adopt/copy/compare + employee "why this balance"; every system template shows "verify with legal advisor before production") → **7I-E** browser QA + soft payroll warning for unverified/missing policy (engine path unchanged).
- **Projects + Tasks / Timelines (Phase 14): ACTIVE — 14A research/spec ✅, 14B DB schema+migration+permissions+seed ✅, 14C oRPC API next.** Plan `docs/architecture/projects-tasks-implementation-plan.md`; DB doc `docs/implementation/projects-db-setup.md`. **14B DB ✅** — `packages/db/src/schema/projects.ts`: **6 tables** (project / project_member [join table — supersedes Horilla jsonb] / project_milestone / project_task / project_task_comment / project_time_entry) + **7 enums** (project_status / project_priority / project_member_role / project_milestone_status / project_task_status / project_time_entry_status / project_linked_entity_type), migration **0017_happy_hammerhead.sql** (purely additive — 6 tables + 7 enums, no existing-table changes; applied; `db:generate` re-run reports no drift). **GUARDRAILS PROVEN via the pg constraint catalog (26/26 DB verify):** `project` has NO CRM foreign key (linkedCustomerId/linkedDealId are plain `text` SOFT refs — crm_* is Phase 17); `project_task.linkedAssetId → asset` and `linkedHelpdeskRequestId → helpdesk_request` are FKs **ON DELETE SET NULL** (read-only context; the 14C router will only READ them); `project_time_entry` has **NO attendance/payroll/payslip FK** (reporting-only — and deliberately **no payrollStatus/lock field**; Finance owns costing/finalization later); project **health is DERIVED** (`PROJECT_HEALTH_STATES` const + type, **no stored column**); **NO `project_activity` table** (Activity reuses the shared `audit_event` log) + generic `linkedEntityType`/`linkedEntityId` (no FK) forward-compat hook. Partial-unique `(org,reference)` + `(org,name)` for projects, `(org,reference)` for tasks, **`(project,employee) WHERE removed_at IS NULL`** (one active membership/employee/project — 0 dups verified). **Permissions** (`packages/auth/src/permissions.ts`): added resources `project`/`task`/`time_entry` to the statement + per-role grants (owner/admin full; hr_admin full+time approve; payroll_admin read+view_costs+time approve; manager read+task-workflow+time approve; employee read+own-task+own-time; auditor read+view_costs+view_internal_notes; recruiter/helpdesk_agent none) + **new `project_manager` role** (added to the roles map → 10 roles; full project/task control + time approve, NOT view_costs; **no existing seed user reassigned** — leads designated via `project_member.role='lead'`). `view_costs`/`view_internal_notes` are the server-side redaction gates for 14C (assets purchaseCost + helpdesk internal-note precedent). **audit:permissions STAYS 93/13** — no router consumes the new pairs yet (the 13B `ticket:approve` precedent); rises to ~106/14 in 14C. **Idempotent seed** `scripts/seed-projects.ts` (Atlas Shipping, runs twice → identical counts): **5 projects** (planning / active×2 / on_hold / completed; the CPE project carries soft `linkedCustomerId=CUST-2026-0007` + `linkedDealId=DEAL-2026-0042`), **11 members**, **8 milestones** (planned/in_progress/at_risk/completed/missed), **25 tasks** (all 6 statuses incl. blocked/in_review/done/cancelled; **1 asset-linked + 1 ticket-linked** read-only), **6 comments** (4 public / **2 internal** for 14C redaction), **8 time entries** (draft/submitted/approved/rejected). Reads one real asset + one real ticket id for the links; **writes nothing outside `project_*`**. Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged** (formatted the 2 drizzle-generated 0017 meta files to match the team convention), audit **93/13 unchanged**, ultracite clean on changed files, migration applied, seed idempotent, DB verify 26/26. **Next: 14C Projects oRPC API** (`projects` router — two-layer authz + lateral scope, tenant-verify every id, budget + internal-note redaction, read-only cross-module link resolution [never write Assets/Helpdesk], time-entry submit/approve/reject [never touch Attendance/Payroll], PRJ-/TSK- reference allocation; verify-projects-api). ── earlier: **14A research/spec ✅ (docs-only)** — Plan `docs/architecture/projects-tasks-implementation-plan.md`; original extraction `docs/architecture/modules/projects-spec.md` + `docs/horilla-extraction/projects.md` (4-entity, jsonb members — **superseded by join tables**). **Central guardrail (user directive): Projects is the COORDINATION layer — link, never own** (mirrors Helpdesk): read-only nullable cross-module FKs ON DELETE SET NULL + generic linkedEntityType/Id, **zero writes** to any other module, money finance-redacted server-side. Suite group **Operations** (with Assets ✅ + Helpdesk ✅). Benchmarked Odoo/ERPNext/Zoho/MS-Project/OpenProject/Redmine/Plane/Taiga/GitLab/Linear/Asana/ClickUp/Monday/Notion/Trello/Jira/ServiceNow/Basecamp/Horilla. **MVP = 6 tables** (project / project_member [join] / project_milestone / project_task / project_task_comment / project_time_entry); **Activity tab reuses shared `audit_event`** (no project_activity table). **Deferred:** dependencies/labels/status_history/Gantt-engine/templates/sprints/files/budget-cost-computation. **New `project_manager` role added in 14B** (absent today — 9 roles). **AC adds resources project/task/time_entry** → audit 93/13 → ~106/14 (1 router + N pairs; role addition doesn't change count). **Coordination links** (mirror helpdesk SET NULL): task.linkedAssetId→asset, task.linkedHelpdeskRequestId→helpdesk_request (helpdesk's enum already lists `project_task`) + generic linkedEntityType/Id; **CRM `project.linkedCustomerId`/`linkedDealId` = SOFT text refs NOT FKs** (crm_* = Phase 17; CRM owns crm_customer_project_link + back-fills projectId). **Time entries reporting-only**; project-time→labour-cost report = **Phase 16 Finance** (reuse payroll-engine + read contract wageType/baseSalary + attendance payrollStatus lock — NEVER mutate attendance/payroll). **PMS seam:** expose task-completion; Phase 15 PMS owns objective↔task link + bonus-point award (Projects writes nothing to PMS). **UI:** KanbanBoard primitive EXISTS (`apps/web/src/components/kanban-board.tsx`, @dnd-kit/core — **needs KeyboardSensor**); TaskChecklist exists but onboarding-coupled (copy/genericise); **Calendar/timeline GREENFIELD** (simple date-ordered milestone/task cards first, NOT Gantt); no shared ModuleTabs (copy assets-tabs/helpdesk-tabs recipe); folder routes day one (no flat stub → route-shadow); sidebar entry budgets for route.tsx a11y/lint debt (lesson #83). Standing UX rules apply (plain labels not raw enums, no raw IDs primary, badges carry text, loading/empty/error triad, :focus-visible #86, aria-labelledby #75, enabled:canView). **Lifecycles:** project planning/active/on_hold/completed/cancelled/archived; task todo/in_progress/blocked/in_review/done/cancelled; milestone planned/in_progress/at_risk/completed/missed/cancelled; priority low/normal/high/urgent; **health DERIVED at read time** (never stored, like SLA). **11 RBAC helpers** byte-aligned; two-layer authz (AC gate + handler scope: seesAll/member/manager-reports via getDirectReportIds(me.id,oid)/self); employee self-service (own task status/complete + own time log/submit; mine/assignedToMe). **Routes** /app/projects/{index,all,$id,$id/tasks,$id/timeline,$id/time,my-tasks,my-time}; detail tabs Summary/Tasks/Board/Milestones/Time/People/Activity. **Seq** 14A spec→14B DB+migration 0017+role+seed→14C oRPC API+verify→14D overview/list→14E detail/members/milestones→14F tasks/Kanban/comments→14G My-Tasks/My-Time/timesheet-approve-reject→14H simple-timeline+activity→14I QA. **Seed** Atlas Shipping (5 projects incl on-hold+completed+1 CRM-soft-link, milestones, ~25 tasks all-statuses, 1 asset-linked + 1 ticket-linked task, public+internal comments, time entries draft/submitted/approved/rejected). **10 open questions answered with recommendations.** Gates (docs-only): check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged** (no code/schema/route/migration touched). **Next: 14B Projects DB schema (6 tables + enums) + migration 0017 + project_manager role + AC resources + idempotent seed.**
- **Helpdesk / Requests (Phase 13): ✅ COMPLETE — 13A spec, 13B DB, 13C API, 13D overview+queue UI, 13E detail/comments/internal-notes UI, 13F employee My Requests self-service, 13G assignment/SLA/approvals workflow, 13H QA/RBAC/security pass.** Plan `docs/architecture/helpdesk-requests-implementation-plan.md`; DB doc `docs/implementation/helpdesk-db-setup.md`; API doc `docs/implementation/helpdesk-api.md`; reviews `docs/reviews/phase-13d|13e|13f|13g|13h/`. **13H QA pass ✅ (CLOSES Phase 13)** — 4 read-only review agents swept security/RBAC/IDOR/redaction · UI/UX/a11y/copy · integration boundaries · API/data-consistency. **Results: integration GUARDRAIL HELD (zero cross-module writes); security NO critical/high/medium; internal-note redaction confirmed server-side in BOTH comments.list AND getById.** Small fixes applied: (1) `getDirectReportIds` gained optional `organizationId` filter (manager-IDOR defense-in-depth) — helpdesk's 4 call sites now pass oid; (2) `assertCanDecideApproval` throws explicitly for non-manager (no "everything-else treated as manager"); (3) overview now has loading/error state (an error no longer looks like a healthy empty desk); (4) `aria-labelledby` added to the 2 dialogs (`ConfirmDialog`, `RequestHelpDialog`) that lacked it; (5) `:focus-visible` outline restored on search/filter/field inputs/pills/tabs/cards (WCAG 2.4.7 — they cleared the default with no replacement); (6) internal-note disclaimer copy → "the helpdesk team and authorized reviewers" (accurate re: auditor/payroll); (7) code comment for the intentional internal-note-on-terminal asymmetry. verify-helpdesk-api expanded **79→96** (cancel lifecycle + authz, every cancelled-terminal transition/comment blocked, internal-note allowed on closed while public blocked, changeStatus→each working state, update+terminal-block, mine:true self-scope of a manage caller). **Browser-verified 7 roles, 11 screenshots `docs/reviews/phase-13h/`** (admin overview/queue/detail; helpdesk_agent full workflow+internal; payroll approve-only; manager scoped; auditor read-only; employee My-requests/detail-no-internal/queue-blocked; recruiter no-access). **Headline guardrails RE-PROVEN via real oRPC client:** employee getById HD-000002 → canViewInternalNotes:false, internalComments:0, REDACTION-PROBE absent + comments.list no-leak; employee non-own getById → FORBIDDEN; **manager scope after the hardening = report getById OK (HD-000085) + non-report FORBIDDEN (behavior-preserved)**. Deferred+documented: reference lexical-MAX robustness (HD-NNNNNN-format safe; (org,reference) unique index is the backstop; only spurious-error risk on imported non-numeric refs), dialog focus-trap/Escape/backdrop (app-wide dialog pattern → cross-app a11y pass), SLA waiting-pause/status_history table, same getDirectReportIds org-filter for onboarding/offboarding/recruitment (param now optional so they're unchanged). Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged**, verify-helpdesk-api **96/96**, web tsc 25 (all pre-existing — 0 in touched files), ultracite clean. 0 app console errors except intentional IDOR/scope probes. **Lesson #86** (`outline:none` without a `:focus-visible` replacement is a recurring WCAG 2.4.7 miss — grep every module's CSS). **PHASE 13 HELPDESK COMPLETE.** ── earlier: **13G workflow ✅** — **API additions all reuse existing `ticket` AC pairs → audit stays 93/13**: `requests.list` gained `assignedToMe`/`unassigned` filters (ticket:read; assignedToMe resolves assignee=caller server-side); new procs `requests.assignToMe`/`unassign`/`assignableAgents` (ticket:assign; assignableAgents returns org members in helpdesk-capable roles as {userId,name,role} only). **Approvals stay on `ticket:approve`, NOT ticket:update** (the user's flagged guardrail — approve ≠ update). verify-helpdesk-api expanded **64→79** (section 9: assignable-agents picker + RBAC, assignToMe lifecycle, assignedToMe/unassigned filters, teammate assign, unassign, employee resolve/approve/assign negatives, manager/auditor assign negatives). UI: new `features/helpdesk/{assignment-controls,approval-panel,request-sla}.tsx`; extended `request-status-actions.tsx` with friendly transitions (Start work/Waiting on employee/Send for approval → changeStatus, hidden for current/terminal); queue gained a "Filter by assignment" select (Assigned to me/Unassigned) wired to the new server flags; employee My cards show `employeeStatusMessage` (plain copy: "We're reviewing this"/"Waiting for your reply"/etc.). Detail wires: **Approval panel** (Approve/Reject; Reject requires a reason; gated canApproveHelpdeskRequest; server enforces manager scope=report/assigned + payroll/HR any) + **Assignment panel** (Assign to me / teammate-picker / Unassign; gated canAssignHelpdesk; hidden when terminal) + **Service-level SLA panel** (derived state + human copy + first-response/resolution due; **waiting-pause NOT implemented — surfaced as an honest note rather than mis-stating targets; no status_history table this phase, deferred**). **Browser-verified 6 roles, 11 screenshots docs/reviews/phase-13g/**: admin full round-trips (assign-to-me→"Sasha Bharrat", assign teammate→"Marcus James", Start work→In progress, Reject-with-reason→"Rejected by Sasha Bharrat — …"); payroll approve round-trip→"Approved by Devon Clarke" (approve/reject only, no assign/status/internal); manager scoped approve on report (Dwayne) + non-report getById **FORBIDDEN** + manager assignToMe blocked (no ticket:assign); auditor read-only zero affordances (sees approval state+SLA+internal, no buttons); employee Cancel-only + public reply + friendly status + **no internal/assign/approval/status-workflow**. **Defense-in-depth (real oRPC client):** employee changeStatus/assignToMe/approve/assignableAgents all AC-blocked (ticket:update/assign/approve/assign); employee public comment OK. 0 app console errors except intentional probes. Gates check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged**, verify **79/79**, web tsc 25 (all pre-existing — 0 in new/changed files), ultracite clean on 10 changed files. Deviations: queue actions read-only (all mutations on detail per plan); payroll approves any category (finance-only restriction deferred); SLA waiting-pause deferred. **Next: 13H** Helpdesk module QA/RBAC/security/browser pass to close Phase 13. ── earlier: **13F UI ✅** — new route `app/helpdesk/my.tsx` (employee self-service): "My requests" list (filter pills Open/Waiting-on-me/Waiting-on-approval/Resolved-closed/All, client-side over one bounded fetch) + "Request help" dialog (plain-language category picker / Summary / "Tell us what happened" / "How urgent is this?"; no target/assignee/internal/approval/link fields) via `requests.createSelf` → toast + navigate to the `$id` detail (reused). **One JUSTIFIED API addition** (spec escape hatch — list scope blocked a truthful self-service surface): `requests.list` gained opt-in **`mine: true`** that forces self-scope (requester = caller) for ANY role; without it "My requests" showed the manager's team scope and the **whole org for admins** (22 rows). Backwards-compatible (defaults off) → verify stays **64/64**, audit **93/13** unchanged. **Self-scope proven** via the real client signed in as the manager: `list({mine:true})` → 3 own (Andre Sealey only); `list({})` → 7 own + report (Dwayne Wilson present in the queue, absent from `mine`). HelpdeskTabs gained a **My requests** tab for canCreate viewers (managers/HR/agents), hidden for read-only auditor/payroll; page gated `canViewHelpdesk||canCreateHelpdeskRequest` with `enabled`/`retry:false` (recruiter → clean "no access", not a generic error). **Employee overview is a LANDING that links to /my, NOT an auto-redirect**: a first attempt used render-time `<Navigate>`, but OrgCtx defaults `memberRole` to "employee" until the async active-membership loads, so the redirect bounced viewers/admins to /my on first paint too — the landing (`EmployeeLanding`, "Go to my requests" CTA) is correct for every role (lesson recorded). **Internal-note redaction re-proven** (employee Rohan's own getById HD-000002 → `canViewInternalNotes:false, internalComments:0, REDACTION-PROBE absent`). Server enforces self-service (auditor `createSelf` → Missing permission: ticket:create; employee other-request `getById` → FORBIDDEN). **Browser-verified 6 roles**, 14 screenshots `docs/reviews/phase-13f/` (employee list/form/create/detail/public-comment/queue-blocked/other-blocked/landing; admin overview-no-redirect + queue-intact + my-self-scoped; manager my own-only; auditor read-only no-create no-my-tab; recruiter no-access). 0 app console errors except noted intentional 403 probes (+ one `<Toast>` insertBefore that was a TEST-HARNESS artifact from manually removing Sonner nodes via evaluate, not a 13F defect). Deviation: linked-context picker omitted from the employee create form (keep self-service simple; links still shown read-only on detail when present). Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged**, verify-helpdesk-api **64/64**, web tsc 25 (all pre-existing — 0 in new files), ultracite clean on changed files. **Next: 13G** assignment + SLA + approvals workflow (Assigned-to-me, approve/reject UI, pause-aware SLA). ── earlier: **13E UI ✅** — route `app/helpdesk/requests/$id.tsx` + `features/helpdesk/{comment-form,request-comments,request-linked-context,request-status-actions}.tsx`; queue title now Links to detail (13D had plain text); helpdesk.css extended (detail/comments/dialog). Detail (helpdesk.requests.getById) = summary grid (category/requester/assignee/logged-by/created/updated/first-response-due/resolution-due/[resolved]/[closed]/[approval]) + description + **read-only Linked-records panel** (only Assets deep-link, gated canViewAssets; others safe label; "Linked for context only — changes must be made in the source module"; NO cross-module mutation) + public **Conversation** timeline + **Internal notes section** shown ONLY when `getById.canViewInternalNotes` + status actions. **SERVER-REDACTED internal notes proven** (the user's flagged guardrail): added an internal note to a request the EMPLOYEE owns, then inspected the employee's own getById via direct RPC → `canViewInternalNotes:false, internalComments:0, REDACTION-PROBE absent from payload` — stripped server-side, NOT DOM-hidden. Comment forms gated: public = canCreateHelpdeskRequest (employee/manager/agents/HR; payroll/auditor excluded = no ticket:create) + not terminal; internal = canManageHelpdesk (ticket:update) + not cancelled. Status actions = canManageHelpdesk for resolve(note required)/close/reopen; cancel = canManageHelpdesk OR employee(requester), while active. RBAC UI: auditor read-only (sees internal, NO forms/actions); manager comment on visible (no internal section, no actions); employee own-only (Cancel + public comment, never internal). Detail query gated `enabled: canViewHelpdesk||canCreateHelpdeskRequest` (recruiter disabled); non-report/non-own → server 403 → "Request not found" EmptyState (retry:false, 1 intentional-probe console error). **Browser-verified 5 roles**, 8 screenshots `docs/reviews/phase-13e/` (admin add public+internal comment round-trip; employee own no-internal + posts public; auditor read-only sees-internal no-forms; manager non-report 403; employee non-own 403), 0 app console errors except noted 403 probes. Deviation: manager-cancels-own not surfaced in UI (cancel gate = canManage||employee; server would allow author cancel; edge case deferred — managers act from queue). Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged**, verify-helpdesk-api **64/64**, web tsc 25 (all pre-existing — 0 in new helpdesk files), ultracite clean on changed files. **Next: 13F employee My Requests self-service** (`app/helpdesk/my.tsx` — own requests list + "Request help" create form via requests.createSelf + reuse detail; replaces the 13D teaser). ── earlier: **13D UI ✅** — `features/helpdesk/{labels,badge,helpdesk-tabs,types}` + routes `app/helpdesk/{index,requests/index}` + `styles/helpdesk.css`. Deleted flat `helpdesk.tsx` stub (route-shadow #4). Added sidebar "Helpdesk" entry (LifeBuoy icon, Operate group) + `helpdesk` to EMPLOYEE/MANAGER visible-key sets (helpdesk_agent already had it; recruiter NOT). HelpdeskTabs (Overview/Requests; returns null for non-viewers). **Overview**: 5 status tiles (Open/In progress/Waiting on employee/Waiting on approval via count queries; Overdue/breached computed from a bounded active-queue fetch + flagged) + "Needs attention" panel (overdue/urgent/waiting-on-approval/unassigned, ≤5 each, real names + plain badges) + queue quicklink. Non-viewers: employee → friendly teaser ("Your requests page is on the way" — My requests=13F); recruiter → no-access EmptyState. **Queue** `/app/helpdesk/requests`: filterable table — server filters status/priority/category/search; **SLA-state filter is client-side** (slaState is derived per-row, not a server filter — refines the current page, pager reflects server total, documented). Columns reference/title+category/requester/status/priority/SLA/assignee/approval(only when required)/updated/Linked-chip. Reference+title are PLAIN TEXT (no $id detail route until 13E → no broken links). **NO mutation UI** (assign/resolve/approve/internal-note) and **NO internal notes shown** anywhere (13E/13G). a11y: aria-labelled filters, text-bearing badges (not colour-only), keyboard-navigable. RBAC UI uses canViewHelpdesk/canCreateHelpdeskRequest; list queries gated `enabled:canView` (no 403 spam). **Browser-verified 6 roles** (admin/agent full queue 10; **manager scoped 1 own — no leak**; auditor read-only all; employee teaser + /requests no-access; recruiter no sidebar entry + no-access), 0 app console errors (only favicon 404), no raw enums/IDs, 7 screenshots `docs/reviews/phase-13d/`. Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged**, verify-helpdesk-api **64/64**, web tsc 25 (all pre-existing, 0 in new files), ultracite clean on 8 changed files. **Next: 13E** request detail + comments timeline + internal-note separation + linked-entity read-only panels + status/resolve controls. ── earlier: **13C API ✅** — `routers/helpdesk.ts` registered `helpdesk` (20 procs: categories list/create/update/archive · requests list/getById/createSelf/createForEmployee/update/assign/changeStatus/resolve/close/cancel/reopen/approve/rejectApproval · comments list/create/createInternal). **Reuses the `ticket` AC** (no `helpdesk` resource); categories gate on ticket:update (no ticket:manage exists). **7 RBAC helpers** role-helpers.ts + byte-aligned rbac.ts (canManageHelpdesk/canViewHelpdesk/canAssignHelpdesk/canResolveHelpdesk/canApproveHelpdeskRequest/canViewHelpdeskInternalNotes/canCreateHelpdeskRequest); local seesAllRequests = canManageHelpdesk∪auditor∪payroll_admin (managers scoped). **Two-layer authz:** AC gate (Better-Auth authorize → "Missing permission: ticket:X", coarse/role-level) + handler re-check + lateral scope (assertRequestVisible / direct-report / requester-self, IDOR-class). **SERVER-SIDE internal-note redaction** (isInternal comments filtered from comments.list AND getById unless canViewHelpdeskInternalNotes=agents/HR/auditor; getById also returns canViewInternalNotes flag). **Derived SLA at read time** (computeSlaState → not_applicable/on_track/due_soon/overdue/breached from dues+status+clock; per-priority SLA_HOURS, category defaultSlaHours overrides resolution; waiting-state pause deferred 13G). **Reference HD-000NNN = MAX+1 per org with retry loop** ((org,reference) unique is backstop). **Guardrail in code: ZERO writes to asset/payslip/payroll_run/leave_request/attendance_record/offboarding_case** — link ids tenant-VERIFIED on write (SELECT-only) + resolved read-only on detail (only asset name resolved; rest typed {kind,id} deep-link refs). Approvals gate on ticket:approve + canApproveHelpdeskRequest + manager-scope (assigned-to-me or report; HR/payroll any); resolve needs note, rejectApproval needs reason. `scripts/verify-helpdesk-api.ts` **64/64** (categories, AC gates by role, IDOR scope, agent lifecycle, approvals, internal-note redaction, link tenant-verify + asset-NOT-mutated, derived SLA). **audit:permissions rose 86/12 → 93/13** (no router used `ticket` before; helpdesk adds 7 ticket pairs + 1 router; all in statement, passes). Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit 93/13, verify 64/64, ultracite clean. **Next: 13D** Helpdesk overview + request-queue UI + HelpdeskTabs + sidebar (delete flat helpdesk.tsx stub = route-shadow #4). ── earlier: **CENTRAL GUARDRAIL (user directive): Helpdesk is the request/ticket LAYER that LINKS to Assets/Payroll/Leave/Attendance/Offboarding — NEVER a second copy of their logic.** **13B DB ✅** — `schema/helpdesk.ts` (**3 tables**: helpdesk_category, helpdesk_request, helpdesk_request_comment; **5 enums**: helpdesk_request_status/helpdesk_priority/helpdesk_category_key/helpdesk_approval_status/helpdesk_linked_entity_type), migration **0016_confused_vulture.sql** applied (clean additive). Guardrail expressed via **6 read-only cross-module link FKs ON DELETE SET NULL** (linkedAssetId→asset, linkedPayslipId→payslip, linkedPayrollRunId→payroll_run, linkedLeaveRequestId→leave_request, linkedAttendanceRecordId→attendance_record, linkedOffboardingCaseId→offboarding_case) + generic linkedEntityType/linkedEntityId (no FK) — context/deep-link only, never mutated. **SLA state is DERIVED not stored** (`HELPDESK_SLA_STATES` const + `HelpdeskSlaState` type for 13C to compute at read time — deliberate deviation from the prompt's enum list, since SLA changes with the clock not a write event; contrast assets currentAssigneeId which IS a write-triggered cache). Partial-unique `helpdesk_category_org_name_uq` + `helpdesk_request_org_reference_uq`; 6 request indexes (status/priority/assignee/requester/target/category) + 2 comment indexes. requesterEmployeeId→restrict (preserve history); category/target/user/link FKs→set null; request→comment cascade. **Decision (user): added `ticket:approve` AC action** (least-privilege — approval ≠ update; granted owner/admin/hr_admin/helpdesk_agent full, payroll_admin read+approve, manager create+read+approve, employee create+read NO approve, auditor read, recruiter none). employee already holds `ticket:create` → **NO new self-service action needed** (unlike assets' asset:request). **audit stays 86/12** (ticket:approve in statement, no router uses it yet). Idempotent seed `scripts/seed-helpdesk.ts` (Atlas Shipping, verified twice): **10 categories** (IT/Facilities/General default-assigned to helpdesk agent, Finance requires approval), **10 requests** (status spread incl 1 urgent+overdue A/C; refs HD-000001..HD-000010; real linked payslip/asset/attendance/leave + generic expense link), **6 comments (4 public / 2 internal)**. DB verified: 5 enums, indexes, category uniqueness, 0 link orphans, comment split. **Reuses the EXISTING `ticket` AC**. helpdesk_agent role already exists. Deferred sla_policy/status_history/attachment/approval-table/KB. Status New→Open→In-progress→Waiting(employee/approval)→Resolved→Closed+Cancelled; priority low/normal/high/urgent; SLA states on_track/due_soon/overdue/breached/not_applicable. Router `helpdesk`; UI HelpdeskTabs + /app/helpdesk/* (DELETE flat helpdesk.tsx stub = route-shadow #4 in 13D; sidebar entry safe now post-12E route.tsx a11y cleanup). Benchmarked Zendesk/Freshdesk/Jira-SM/Frappe-ERPNext/GLPI/ServiceNow/Horilla. 13A→13H. Gates: check-types 3/3, build 2/2, ultracite clean on changed files, audit 86/12. **Next: 13C API** (router `helpdesk`, reference max+1 transactional, two-layer authz, server-side internal-note redaction, derived SLA, approve gated on ticket:approve).
- **Assets (Phase 12): ✅ COMPLETE (12B DB → 12C API → 12D UI → 12E QA/hardening, HEAD d9591f1).** 12E: fixed route.tsx's 6 pre-existing lint/a11y errors (empty catch→commented; menu-trigger divs→keyboard handlers + useSemanticElements biome-ignore; closeAll→useCallback) → added **"Assets" sidebar entry**; lint 218→212. Read-only **offboarding custody panel** (`features/assets/custody-panel.tsx`, uses existing assets.assignments.listByEmployee → no new API, audit 86/12) on offboarding case detail — live custody, no write-back, no mutation, no purchaseCost, note "Asset returns tracked in Assets; offboarding clearance manual/read-only". Browser-verified all roles + custody panel (empty+populated, auditor read-only) + manager cost-redaction holds + RPC RBAC, 0 console errors, 5 screenshots docs/reviews/phase-12e/. Gates check-types 3/3, build, lint 212/1/1, audit 86/12, verify-assets-api 46/46. **Next per roadmap: Phase 13 Helpdesk/Requests.** ──── (history) **Assets (Phase 12) build log below was the active-phase record.** Spec `docs/architecture/assets-implementation-plan.md`. **12B DB ✅** — `schema/assets.ts` (4 tables asset_category/asset/asset_assignment/asset_request + 3 enums asset_status/asset_return_condition/asset_request_status), migration **0014_fat_shiver_man.sql** applied; partial-unique invariants `asset_org_tracking_uq`, `asset_category_org_name_uq`, **`asset_assignment_open_uq`** (≤1 open assignment/asset); `asset.currentAssigneeId` denormalised cache. **12C API ✅** — `routers/assets.ts` (registered `assets`). Resolved the open question: added **`asset:request`** AC action (employee held only `asset:read`) granted to owner/admin/hr_admin/payroll_admin/manager/employee/recruiter (NOT auditor/helpdesk); self-service gates on it — avoids the markUploaded manage-gate trap. 6 helpers in role-helpers.ts + rbac.ts (canManageAssets/canViewAssets/canAssignAssets/canReturnAssets/canRequestAsset/canViewAssetCosts). Inventory ops at router root (list/getById/create/update/retire/writeOff) + `categories`/`assignments`/`requests` sub-resources. **Server-side purchaseCost redaction** (`redactAsset`, finance/audit only). **Transactional** assign/return via shared `performAssign`; status+currentAssigneeId are single-writer derived caches (assign/return/retire only; `update` refuses them); major_damage return auto-retires; history preserved. Two-layer authz (tenant FK verify + manager direct-report / employee-self). Offboarding link read-only (`assignments.listByEmployee`), no write-back. Verify `scripts/verify-assets-api.ts` **46/46**; gates check-types 3/3, build, lint 223/1/2 unchanged, audit **80/11**. Doc `docs/implementation/assets-api.md`. **12D Assets inventory UI ✅** (commit 8ce656d): removed flat assets.tsx stub; `features/assets/{labels,assets-tabs}` + routes `app/assets/{index,inventory/index,inventory/$id,requests,my,categories}` + assets.css. Inventory table (filters/search/pagination; purchaseCost col only when canViewAssetCosts), detail (assign/return/retire dialogs, major-damage auto-retire warning), requests queue (approve/reject-reason/fulfil), employee "My assets" (custody+requests+request dialog, no cost), categories. API added `assets.assignments.listMine` (zero-arg self custody, reuses asset:read → audit **86/12**) + `fetchOpenCustody`. Fixed picker pageSize 200→100 (schema cap → was 400). Browser-verified all roles + RPC RBAC (employee mutations 403, listMine/createSelf 200; auditor read-only), manager cost-column redacted (0 leak), 0 console errors, 10 screenshots docs/reviews/phase-12d/. Gates check-types 3/3, build, lint 218/1/2, audit 86/12, web tsc 0 touched-file, verify-assets-api 46/46. **Deferred to 12E:** "Assets" sidebar entry — touching shared route.tsx hits pre-existing a11y/lint debt the pre-commit hook blocks (lesson #83); fix in 12E. Module reachable at /app/assets + tab-navigable. **Next: 12E Assets QA/hardening** (sidebar+route.tsx a11y; read-only offboarding custody panel; requests/offboarding QA).
- **Biometric + Geofencing (Phase 11): ACTIVE.** 11A spec ✅; 11B DB ✅ (`schema/biometric.ts`, 8 tables + enums, migrations `0011`/`0012`); **11C sync/import API + processor ✅** — `routers/biometric.ts` (devices/mappings/syncRuns/punches/processor/ingest/geofences/assignments/checkIns/exceptions). **Multi-vendor adapter/provider model** (`utils/attendance-adapters.ts`): ZKTeco TCP/ADMS + NGTeco cloud/app/K-series + generic CSV/Excel/USB/API; live ZKTeco/NGTeco-cloud are `planned` (no faked sync). Source-agnostic idempotent processor (`utils/biometric-processor.ts`) bridges staged punches → `attendance_event` → `recalculateRecord` (extracted to `utils/attendance-recalc.ts`). Public `ingest.submit` = device API-key auth. 4 AC resources + 6 RBAC helpers. **Privacy:** no biometric templates ingested/stored; device secrets never returned. audit:permissions 73/10. **11D devices UI ✅** — `features/biometrics/*` + `routes/app/biometrics/{index,devices,devices/$id,sync-runs,punches}` + biometrics.css (flat stub removed). Overview/devices-list/detail/sync-runs/punches with a **Supported now / Planned** badge, honest planned-adapter states (no fake live sync), CSV-paste import dialog, secrets never rendered. Browser-verified admin/auditor/employee, 0 app console errors, screenshots in docs/reviews/phase-11d/. **11E punch review + exception queue UI ✅** — `/app/biometrics/punches` (Punch review: filters, Map-unmapped dialog, Process-pending confirm, payroll-safety note) + `/app/biometrics/exceptions` (queue: filters, Acknowledge/Resolve/Dismiss with required notes); `exceptions.list` enriched with resolutionNote. RBAC UX (canManageBiometrics/canReviewAttendanceExceptions); auditor read-only; employee no-access. Browser-verified incl. resolve round-trip; 0 app console errors (fixed a pageSize-200→400 bug). Lesson #76 addendum: `--hot` also won't reload handler-body/select changes → restart API server. **11F geofence locations + mobile check-in UI ✅** — `/app/geofencing{,/locations,/locations/$id,/check-in}` + GeofencingTabs + MobileCheckIn + location form dialog; new read-only `checkIns.previewSelf` (verdict preview, reuses geofence:check_in). Mobile-first, non-technical worker wording; tap-only geolocation (no background tracking); soft-block-outside-with-reason; friendly permission-denied; no raw GPS coords shown to workers. RBAC: canManageGeofencing/canViewGeofencing/canUseGeofenceCheckIn; auditor read-only; employee → check-in only. Browser-verified inside/outside/denied + blocked-from-management; 0 app console errors. **11G integration hardening IN PROGRESS — CP1 work-arrangement modes ✅** (migration 0013: work_arrangement enum + employee_work_info column; arrangementPolicy: only onsite enforces fence/raises exceptions/requires GPS; remote/hybrid/field/exempt never auto-flag outside → won't block payroll; mobile check-in shows friendly "Remote work check-in" + GPS-optional; verified remote check-in raises NO outside_geofence exception). **CP2 payroll readiness from attendance exceptions ✅** — open biometric/geofence/attendance exceptions surface as payroll blockers/warnings (never silently change pay). Builder `buildExceptionReview` period-scopes open/in_review exceptions (linked punch/event/check-in date → createdAt fallback), tallies `openExceptionBlockers`/`openExceptionWarnings`/`unprocessedPunches`; `flags.blockPayrollOnOpenExceptions` from `attendance_setting` (default true). Engine (pure) emits `UNRESOLVED_ATTENDANCE_EXCEPTION` (blocker) / `ATTENDANCE_EXCEPTION_REVIEW` (warning, downgrades blockers when policy off) / `UNPROCESSED_PUNCHES_FOR_PERIOD` (warning). `runs.confirm` gates on a **live open-blocker `payroll_issue` count** (not cached `blockerCount`). Plain-language UI labels + "raw device punches don't go directly to payroll" note. Worked minutes still read only from `attendance_record`. Verified: `scripts/verify-biometric-payroll-readiness.ts` 11/11 + browser preview (blocker under "cannot continue", `runs.confirm` 412, RBAC: employee/auditor denied, auditor confirm 403); audit 73/10. **CP3 live/projected pay hardening ✅** — projected pay is an estimate (never finalization) that now expresses a confidence level from the CP2 signals. New `payroll-engine/src/confidence.ts` = single source of truth (logic was duplicated in calculate.ts + projected-pay.ts and stale w.r.t. CP2 exception fields). `confidenceLabel`: High confidence / Needs review / Cannot finalize yet. `calculateProjectedPay` returns richer result (confidenceReasons[], warnings[], blockers[], hours/days, payType, attendanceComplete; disclaimers[0] = not-final guardrail; paid leave never reduces monthly base). API: `projectedPay.forEmployee`/`.own` — payPeriodId optional (defaults current period), `.own` strips resolutionLink (no admin links to employees), `runs.preview` returns confidenceCounts. UI: employee "Estimated pay this period" card on /app/payroll/payslips + admin "Projected pay confidence" indicator in run preview. Verified `scripts/verify-live-pay-projection.ts` 13/13 + projected-pay.test.ts (engine 27/27) + browser (employee card Cannot-finalize/Needs-review, admin preview counts, own payload no resolutionLink, auditor forEmployee 403). Hourly/daily covered by unit tests (none seeded — limitation logged; "shift"=daily, no enum). **CP4 GPS scrub + source labels + integration verify ✅ (closes 11G)** — no migration (schema already had nullable geofence lat/lon + coordsPurgedAt + attendance_setting.gps_retention_days=90). `scripts/scrub-geofence-gps.ts`: after retention window, NULL geofence_check_in lat/lon + stamp coordsPurgedAt (keep verdict/distance/accuracy/site/reason/time) + NULL attendance_event locationLat/Lon; DRY-RUN default, --apply to write, recent rows untouched, prints counts. Attendance `recordsList` enriched with source key (from linked events: single/mixed/none) + needsReview (open exception on employee+date); UI maps to plain labels (Manual entry/Biometric device/Mobile GPS check-in/File import/Admin adjustment/Mixed sources/Source unavailable) + "Needs review" badge, no raw enum/GPS. Verified `scripts/verify-biometric-geofence-attendance-payroll-integration.ts` 22/22 (raw-punch safety, exceptions, readiness, projection, privacy/scrub, processor link+idempotency) + scrub dry-run/apply proof + browser (source labels, no lat/lon leak, estimated card, geofence check-in, employee 403 on exceptions/punches, auditor validate 403). **Phase 11G COMPLETE.** **11H ✅ — PHASE 11 (Biometric + Geofencing) COMPLETE.** Module-wide QA/RBAC/security/browser pass (3 parallel read-only audits → sequential fixes only). Fixes: `payslipsList` leftJoins employeeProfile (names not raw cuid in payslips + run preview, via getTableColumns); wageType→wageTypeLabel; correction status→label; biometrics overview "Geofencing (coming soon)" <a> → <Link>; `containsBiometricTemplate` now recurses nested objects/arrays (template under meta.fingerprintData was accepted); `ingest.submit` rejects non-active devices; `buildExceptionReview` compares period bounds on DATE granularity (date-mode vs timestamp dropped last-day exceptions — lessons #80). Documented backlog (not changed in QA): manager mappings/assignments visibility (A1/A2), processor non-transactional double-create window (C-D1), unmapped-punch per-employee gap (C-D2), duplicate_punch dead type (C-D3), event-scrub date granularity (C-D5). RBAC verified live: employee global punches/exceptions 403 + own projection no resolutionLink; auditor read-only (mutations 403, no action buttons); payroll_admin/manager no device/geofence manage; public ingest bad key 401 + nested template rejected + inactive device 403. Gates: check-types 3/3, payroll-engine 27/27, build, web tsc 26 baseline, ultracite 223/1/2, audit:permissions 73/10. 12 screenshots docs/reviews/phase-11h/, 0 console errors. **Next: Phase 12 Assets** (queued spec, connects to Offboarding asset returns); user roadmap order after: Helpdesk/Requests → Projects/Tasks/Timelines → Performance → Finance → CRM. Future modules (CRM/Finance/Projects/Tasks) NOT built yet; CRM spec must research Odoo/ERPNext/TwentyCRM/SuiteCRM/EspoCRM/Vtiger + HubSpot/Salesforce/Zoho/Pipedrive (+ Horilla/OpenHRMS where HR∩CRM).
- HR Core (Phase 5): **Complete**
- Contracts (Phase 6): **Complete** end-to-end
- Phase 6E: **Complete** — payroll/attendance/leave spec enrichment, GRA verification
- Attendance + Leave (Phase 7): **Complete** through 7H (QA/RBAC pass)
- Payroll (Phase 8): **Complete** through 8K + 8J.1/8J.2/8J.3 polish + correctness fixes
- Recruitment + Onboarding (Phase 9): **Complete** (through 9I QA/hardening)
  - 9A (spec) ✅ → 9B (DB schema + seed) ✅ → 9C (oRPC API, 50 procedures) ✅ → 9C.1 manager-scope IDOR fix ✅
  - 9D (Recruitment UI) **CHECKPOINT 2** ✅ + cleanup (`5004616`): Overview + Jobs list/detail + Candidates list + KanbanBoard primitive (@dnd-kit/core only) + Pipeline page (job-scoped, drag/Move-menu, Reject dialog, one-step-backward policy, plain-language errors, `onMoveError` prop).
  - 9D **CHECKPOINT 3** ✅: Interviews list (status filter, client-side join, badges, EmptyState) — live-verified.
  - 9D **CHECKPOINTS 4–6** ✅ (all live-verified via Playwright, 0 console errors): **Offers list+detail** (compensation redaction — recruiter/auditor see "Compensation hidden", owner/admin/hr_admin/payroll_admin see amounts; no create/send/approve); **Reports** (6 stat tiles + pipeline-by-stage CSS bars + offer outcomes, gated owner/admin/hr/auditor); **Candidate detail** (Profile/Applications/Interviews/Notes/Documents tabs, PII shows "Hidden" when redacted, notes/docs FORBIDDEN degrade gracefully).
  - 9D **CHECKPOINT 7** ✅ (live-verified): **Job create/edit forms** (`JobFormDialog`) + **Job detail Overview/Settings tabs** + **status transitions** (Open/Pause/Close/Cancel with confirm dialogs + plain-language helper text; valid-transition-only, no reopen). Compensation/PII-style safety: only API-validated/scalar fields exposed; departmentId/jobPositionId/recruiterUserId omitted (not server-verified — 9I follow-up). **Backend bug found+fixed**: `posting:update` was missing from the AC statement so no role could edit a job (commit `bad8910`).
  - 9D security fix (`357d590`): stored-XSS guards — `safeHttpUrl()` on all DB-derived hrefs + `httpUrlString` Zod validator server-side.
  - 9D **CHECKPOINT 8** ✅ (live-verified): **Interview row actions** (`InterviewActions`) — Reschedule / Mark completed / Cancel / Mark no-show (scheduled-only, confirm dialogs) + Add feedback (completed-only; interviewer names via `hrCore.employees.list`; excludes already-submitted; rating 1–5 + friendly recommend) + View feedback (read-only). Valid-action-only per status; auditor read-only, employee no access. **API bug fixed**: duplicate-feedback now returns CONFLICT not 500 (Drizzle wraps pg error on `.cause`).
  - 9D **CHECKPOINT 9** ✅ (live-verified): **Pipeline candidate quick-view drawer** (Decision-B hybrid) — clicking a card's candidate name opens a right-side drawer (name/status/email/phone/added + "Open full profile →" to `/candidates/$id`); name is a stopPropagation button so drag is unaffected (body-drag still works, no accidental opens). `.qv-*` styles in recruitment.css.
  - 9D **QA/SECURITY PASS** ✅ (`53c8e87`): gates green (lint 225, check-types, build); href audit — all DB-derived hrefs `safeHttpUrl`-guarded; no raw enum leakage (all status/stage/recommend via label maps); fixed 2 internal-ID-as-text spots (jobs/$id candidate names; notes "Team member"). **RBAC matrix verified secure**: Reports gated to owner/admin/hr/auditor; recruiter/manager no Reports; employee/payroll no recruitment sidebar entry; every non-viewer data query 403s (no leak). Known minor (deferred): direct-URL nav to recruitment by a non-viewer shows a degraded Overview firing 403s (sidebar already hides it, data safe) — add a recruitment route-group `canViewRecruitment` guard in 9I; manager has scoped API view access but no sidebar entry (confirm intended).
  - 9D is **feature-complete + QA-passed**. Added `scripts/audit-permissions.ts` (`bun run audit:permissions`) — a QA gate that prevents the `posting:update` class of bug; passes (35 pairs/7 routers), negative-tested.
  - **9I hardening backlog** captured in `docs/architecture/phase-9i-hardening-backlog.md`: (1) permission audit ✅ done; (2) job FK server-verify + expose Department/JobPosition/Recruiter selectors; (3) API denormalization of candidate/opening display fields (5 pages); (4) dialog a11y (aria-labelledby/describedby, focus trap/return, Esc); (5) standardize detail-page tabs (Overview/Activity/Documents/Settings); (6) readable status-history timelines; (7) recruitment route-group no-access guard; (8) brand-new interview scheduling on pipeline/candidate flow.
  - 18/18 payroll-engine tests, 225 lint baseline maintained.
  - **Browser verification VERIFIED ✅ (2026-05-29)** via Playwright on `:3002` against a healthy local API (DB password synced from Infisical into `apps/server/.env`; full process restart). Confirmed: recruiter login → pipeline loads → job picker → drag forward (new→screening) → drag one-back (screening→new) → Move-menu one-step rule (3-back blocked with toast "Move back one stage at a time."; 1-back allowed) → Reject dialog submits + candidate leaves board + Rejected count increments → 0 steady-state console errors. Role matrix: owner/admin/hr_admin/recruiter manage (Move+Reject present); auditor read-only (board visible, 0 Move/Reject); manager scoped (Reports tab hidden, sees only managed openings); employee no access (Pipeline tab hidden).
  - **Defect found + fixed during verification**: the "Move to" menu's `onSelect` bypassed `canMoveStage` (only the drag path enforced the policy). Fixed so the menu applies the identical guard + toast. (`pipeline.tsx`)
- 9E (Onboarding DB schema + seed) ✅: `packages/db/src/schema/onboarding.ts` — 7 tables (onboarding_template, onboarding_template_task, employee_onboarding, onboarding_task, onboarding_document_request, onboarding_acknowledgement, onboarding_activity) + 4 enums (onboarding_status / onboarding_task_status / onboarding_category / document_request_status); migration `0009_complete_red_hulk.sql` applied; `scripts/seed-onboarding.ts` (3 templates/24 tmpl-tasks/5 instances across all statuses/55 tasks/12 doc-requests/2 acks/16 activities). Snapshot rule: onboarding_task denormalizes title/description/category from template (template edits don't mutate in-flight onboardings). Partial unique (org,name) where not-deleted, enforced. Gates: check-types/build/lint-225/audit:permissions all ✓. See `docs/implementation/onboarding-db-setup.md`.
- 9F (Onboarding oRPC API) ✅: `packages/api/src/routers/onboarding.ts` — 7 groups / 31 procedures (templates, templateTasks, employeeOnboarding, tasks, documentRequests, acknowledgements, activity). New `onboarding` resource + 10 actions in permissions.ts; `canManageOnboarding`/`canViewOnboarding` helpers (backend + frontend). **Transactional `start`** (onboarding + snapshot tasks + activity commit together — no half-started onboarding); tenant-verify every FK; employee self-scope (own onboarding/tasks/acks) + manager/auditor scope; audit on every action; plain-language errors. Verified end-to-end via `scripts/verify-onboarding-api.ts`. Gates: check-types/build/lint-225/**audit:permissions (45 pairs/8 routers)** all ✓.
- Next: **Phase 9G Onboarding UI** (`/app/onboarding` routes, OnboardingTabs, TaskChecklist primitive, progress/overdue, document collection, acknowledgement, employee-friendly view). Then 9F–9G Onboarding API/UI, 9H candidate→employee conversion, 9I QA + API denormalization (candidate/opening display fields in interviews.list/offers.list/applications.list) + server-verify departmentId/jobPositionId/recruiterUserId before exposing in job forms + brand-new interview scheduling on the pipeline/candidate flow + recruitment route-group no-access guard.

### Product Standards (set during Phase 8J.1, extended in 8J.2)
- **Module tabs are a product standard.** Each multi-page module exposes its sub-pages via a tabs strip immediately under the page header (`PayrollTabs` in `apps/web/src/features/payroll/payroll-tabs.tsx` is the reference). Future module tabs are recommended for Attendance, Leave, Employee Profile, and Contracts.
- **Plain-language UX.** Never surface raw enum or audit codes as primary text. "Blocker" → "Needs fixing / Cannot continue". "Warning" → "Needs review". "draft" → "Preview". "confirmed" → "Finalized". Raw codes may appear as small secondary text for debugging.
- **No payment automation.** "Marked as paid" only after manual bank confirmation. Exporting a bank file is not payment. No bank-specific export format without official documentation.
- **RBAC role normalization.** Import role helpers from `apps/web/src/lib/rbac.ts` (frontend) and `packages/api/src/utils/role-helpers.ts` (backend) — `canManageHR`, `canManagePayroll`, `canViewPayroll`, `isOwnerOrAdmin`. Helpers accept both Better Auth's default role strings (`owner`, `admin`) and our custom ones (`tenant_owner`, `tenant_admin`). Never re-declare inline `PAYROLL_ROLES` / `HR_ROLES` arrays.
- **Empty states vs skeletons.** Skeleton rows render only while `isLoading === true`. Once data has loaded and the result is empty, render `<EmptyState />` from `apps/web/src/components/empty-state.tsx` with `title` + `description` + (optional) primary action. Never use skeletons as a permanent empty display.

### Key Architecture Files
- `.claude/CLAUDE.md` — Full project instructions with doc references
- `docs/architecture/` — Schema specs, API specs, UI plans, implementation plans
- `docs/horilla-extraction/` — Module extraction docs from Horilla/OpenHRMS
- `packages/db/src/schema/` — Drizzle schema (`auth.ts`, `hr-core.ts`)
- `packages/api/src/routers/` — oRPC routers
- `packages/api/src/utils/` — Audit, scope, error utilities
- `packages/auth/src/permissions.ts` — 9 tenant roles with resource/action definitions

### Design Fidelity Rule
- Handoff CSS classes (`.tbl`, `.badge`, `.tabs`, `.filter-chip`, etc.) are first choice
- shadcn used only for behavior/accessibility where handoff has no equivalent
- No visual drift toward default shadcn styling

---

## Permanent execution model (phases 9G → 15)

Source of truth for **how** work is executed. Applies to every remaining phase.

- **Parallelism = read-only review only.** Use Workflows / parallel subagents only for
  read-only review (UX, security/RBAC, a11y, docs consistency, screenshots, QA plans,
  audits). Never run multiple agents that edit code at once — only one agent writes.
- **Implementation is sequential.** One implementation agent writes code, checkpoint by
  checkpoint. Never two writers on the same module or shared file
  (`role-helpers.ts`, `rbac.ts`, `permissions.ts`, `*.css`, `routeTree.gen.ts`, API
  routers, DB schema, `packages/ui/*`, `packages/payroll-engine/*`). No mega-commits.
- **Commit / push / verify clean tree after every checkpoint.**
- **Quality gates every checkpoint:** `bun run check-types --force --filter=web`,
  `bun run build --force --filter=web`, `bun x ultracite check` (hold the documented
  225/1 baseline — never *regress*; the baseline itself is pre-existing and accepted),
  `bun run audit:permissions` (must PASS).
- **Verification is type-specific and mandatory:** UI → Playwright (real DOM events;
  authenticate via same-origin `/api/auth/sign-in/email`; 0 console errors on the happy
  path) + screenshots under `docs/reviews/<phase>/`; backend → authenticated RPC (authz
  denial *before* existence checks); schema → migration. `browser_run_code_unsafe` runs
  in Node — DOM/`fetch`/timers only exist inside `page.evaluate(...)`. Never skip
  verification just because the build passes.
- **Security:** frontend checks are UX only, API is the source of truth; tenant-verify
  every FK; employee self-scope enforced server-side; auditor read-only everywhere.
- **Hard stops:** stop and report before touching shared credentials / secrets / central
  DB passwords / Infisical; never commit `.env`; restart only your own project's local
  dev processes (confirm a process's cwd before killing it).
- **Dev env:** API :3000, web must bind :3002 (vite proxies `/api`+`/rpc` → :3000);
  test org `atlas-shipping`; creds `*@atlas-shipping.com` / `HeimdallTest2026!`.

### Phase roadmap
Canonical build order lives in
[docs/architecture/modules/implementation-sequence.md](docs/architecture/modules/implementation-sequence.md).

✅ 5 HR Core · ✅ 6 Contracts · ✅ 7 Attendance + Leave · ✅ 8 Payroll ·
✅ 9 Recruitment + Onboarding · ✅ 10 Offboarding →
**11 Biometric + Geofencing ← NEXT** (11A spec) →
12 Assets / Helpdesk / Projects (Assets spec already drafted + queued —
`docs/architecture/assets-implementation-plan.md`) →
13 Performance / PMS → 14 Automations + Notifications →
15 Analytics + Dashboards + Reports.

Each phase still runs the A→E checkpoint cadence (A spec → B schema+seed → C API →
D UI → E QA/RBAC); Biometric + Geofencing fans 11A–11H per the spec.
