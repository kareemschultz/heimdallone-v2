# Implementation Sequence — Module Build Order

## Recommended Order

```
Phase 5B:  HR Core (employees, org settings, documents, audit) ✅
Phase 6:   Contracts ✅ (6A-6E complete, verified end-to-end)
Phase 7:   Attendance + Leave ✅  (+ 7I Leave Policy Engine / Statutory Library — A/B/C/D/E ✅ COMPLETE)
Phase 8:   Payroll ✅ (8A–8K + 8J.1 module-tabs / UX clarity polish + 8J.2 role normalization)
Phase 9:   Recruitment + Onboarding ✅ COMPLETE (9A–9I, verified end-to-end)
Phase 10:  Offboarding ✅ COMPLETE (10A spec, 10B DB, 10C API, 10D UI CP1 overview + CP2 templates + CP3 cases + CP4 checklist actions + CP5 employee self-service + CP6 QA/RBAC pass)
Phase 11:  Biometric + Geofencing ✅ COMPLETE (11A spec → 11B DB → 11C API/processor → 11D devices UI → 11E punch/exception UI → 11F geofence/check-in UI → 11G integration hardening CP1–CP4 → 11H module QA/RBAC/security/browser pass ✅). Next = Phase 12 Assets (queued spec; connects to Offboarding asset returns).
Phase 12:  Assets ✅ COMPLETE (Operations; 12B DB → 12C API → 12D UI → 12E QA/hardening+sidebar+offboarding-custody)
Phase 13:  Helpdesk / Requests ✅ COMPLETE (Operations; 13A spec → 13B DB → 13C API → 13D-13G UI → 13H QA/RBAC/security pass) — request/ticket LAYER that LINKS to Assets/Payroll/Leave/Offboarding, never duplicates them
Phase 14:  Projects + Tasks & Timelines ✅ COMPLETE (Operations; 14A spec → 14B DB → 14C API → 14D-14H UI → 14I QA ✅) — coordination layer (link, never own)
Phase 15:  Performance / PMS ← ACTIVE (People&Payroll; 15A spec ✅ → 15B DB ✅ → 15C API ✅ → 15D-15H UI → 15I QA) — owns performance data, links read-only (points ≠ pay; reads project task-completion, never writes Projects/Payroll)
Phase 16:  Finance expansion (Finance — payment batches, bank exports, expenses, costing)
Phase 17:  CRM (research/spec at 17A — see crm-implementation-plan.md; FUTURE)
Phase 18:  Analytics / Executive dashboards
Phase 19:  Enterprise QA / accessibility / security hardening
Phase 20:  Production readiness
```

> **Long-term suite direction (user directive 2026-06-02).** Heimdallone is one
> platform organised into **five product groups**: **People & Payroll · Operations ·
> Finance · CRM · Admin & Compliance**. Modules are documented and queued under their
> group but kept **hidden/queued in the sidebar until their phase begins** — do not
> overload the nav with future modules.
>
> | Group | Modules |
> |---|---|
> | **People & Payroll** | Employees · Contracts · Attendance · Leave · Payroll · Biometrics & Geofencing · Recruitment · Onboarding · Offboarding · Performance · Training |
> | **Operations** | Assets · Helpdesk / Requests · Projects · Tasks & Timelines · Timesheets · Field work |
> | **Finance** | Payroll payment batches · Bank exports · Expenses · Reimbursements · Loans & advances · Department/job costing · Accounting integrations |
> | **CRM** | Leads · Customers · Contacts · Deals / opportunities · Sales pipeline · Activities · Client handoff to projects |
> | **Admin & Compliance** | Organization settings · Roles & permissions · Audit logs · Countries & tax · Policies · Document templates · Reports / dashboards |
>
> **Recommended order after Phase 11:** 12 Assets → 13 Helpdesk/Requests →
> 14 Projects + Tasks & Timelines → 15 Performance/PMS → 16 Finance expansion →
> 17 CRM → 18 Analytics/Executive dashboards → 19 Enterprise QA/accessibility/security
> hardening → 20 Production readiness.
>
> **Future modules — DO NOT implement yet:** CRM, Finance, Projects, and Tasks are
> future-only. Document the roadmap; keep them queued/hidden. **CRM is the furthest
> out**: when Phase 17 begins, **17A is research/spec-only** (no code) — see
> [crm-implementation-plan.md](../crm-implementation-plan.md). Do NOT build CRM from
> scratch by guessing; the 17A plan synthesises Odoo, ERPNext, TwentyCRM, SuiteCRM,
> EspoCRM, Vtiger, YetiForce/OroCRM, HubSpot, Salesforce, Zoho, Pipedrive, Attio,
> Dynamics 365 (+ Horilla/OpenHRMS only where HR ops intersect CRM). The CRM thesis
> for Heimdallone is the cross-suite flow **Lead → Customer → Deal → Project → Staff
> assignment → Timesheets → Payroll cost → Invoice/report** — CRM exists to connect
> sales to people/operations/payroll cost, not just to track sales. Do not implement
> CRM until People/Payroll + Assets + Helpdesk/Requests + Projects foundations are
> stable unless explicitly told to move it earlier.
>
> **Roadmap correction (2026-06-02):** Phase 11 was **Biometric + Geofencing** (now
> COMPLETE), not Assets. The Assets plan
> ([assets-implementation-plan.md](../assets-implementation-plan.md)) was drafted
> early; it is now the **active Phase 12**.

## QA Hardening Pass (2026-06-04) — nav, gates, roadmap, placeholders

A repo-wide static-audit follow-up landed between **14B (Projects DB) and 14C
(Projects API)**. It is QA/scaffolding only — **no schema, router, or business-logic
change** (audit:permissions stays 93/13; check-types 3/3; build 2/2).

**Fixed (safe):**

- **Nav leak closed.** `isNavItemVisible()` had a blanket `return true` fallthrough
  that exposed every nav key to the new `project_manager` role. Added an explicit
  `PROJECT_MANAGER_VISIBLE_KEYS` allow-set (overview · employees · contracts ·
  attendance · leave · assets · helpdesk · documents · settings) checked before the
  fallthrough. (Other roles already had explicit key-sets or `canViewPayroll`.)
- **Preview/Planned labelling.** Compliance, Clients, Countries & Tax, and Documents
  are **design scaffolds wired to sample/demo data, not live backends**. They now show
  a **"Preview" sidebar pill** and a top **`<PreviewBanner>`** ("Preview module —
  design scaffold only … sample/demo data … not used for live compliance, payroll,
  audit, or reporting"). Routes are **kept reachable** for design review — nothing
  deleted. Compliance specifically: hardcoded stats relabelled "sample/demo ·
  illustrative", Export / Evidence-pack buttons **disabled + "(preview)"**.
- **Topbar sync chrome de-spoofed.** "Last HR sync · 14:42 / Horilla HRMS sync /
  Operational" → "Demo sync status / Integration status (preview) / Demo" + a
  "Sample data — not connected to a live HRMS sync" note. No real integration is
  implied.
- **Stale roadmap header** corrected (Helpdesk ✅ COMPLETE; Projects ACTIVE).
- **Sign-in / sign-up redirect** `/dashboard` → `/app` (dead route).
- **Clean web tsc reductions** in the payroll routes (Boolean() guards for
  unknown→ReactNode, payslips Link `params`, ReadinessStep prop, settings val type) —
  web tsc **25 → 7**.

**CI added** (`.github/workflows/ci.yml`): install + `check-types` + `build` +
`audit:permissions` are **blocking**; `check` (ultracite lint) and web `typecheck`
run **`continue-on-error` (informational)** so they surface regressions in PR logs
without blocking merges until the baselines below are burned down.

**Documented baselines + follow-up hardening task (NOT regressions):**

- **Web type-check is not in the root `check-types` gate.** `apps/web` has no
  `check-types` script (its script is `typecheck`), so `turbo check-types` only
  aggregates server/ui/payroll-engine (**3/3**). Wiring web in would red the root gate
  on a pre-existing **7-error baseline** concentrated in legacy
  `docs.tsx` · `app/settings.tsx` · `employees/$id.tsx` · `employees/index.tsx` ·
  `login.tsx` (unused vars + one Date→string cast + one stale route literal).
- **Lint baseline 212.** `bun run check` exits non-zero on accepted pre-existing
  a11y / cognitive-complexity / nested-ternary debt in those same large shared files.
- **Why not fixed here:** the pre-commit hook runs `ultracite fix` on the **whole
  staged file**, so editing a debt-carrying file surfaces its full pre-existing error
  set and hard-blocks the commit (lesson #83). Fixing these requires a dedicated
  burn-down pass per file, not a drive-by edit. **Follow-up task: clear web tsc 7 →
  0 and wire `apps/web` into the root `check-types` gate; then make CI lint blocking.**
- **Bank data (finding 8):** no plaintext leak at the API boundary —
  `employee_bank_details.account_number` is masked on read for all non-payroll roles
  (`bankDetailsGet`), full access gated to HR/payroll admins, and payroll masks via
  `maskAccountNumber`. The column is plaintext **at rest** (not field-level
  encrypted) — a documented limitation, not a response-layer leak; at-rest encryption
  is a separate future hardening item.

### Phase Status (2026-05-28 — Phase 9A complete)
- **Phase 5**: ✅ HR Core MVP complete (employees, org settings, holidays, RBAC)
- **Phase 6**: ✅ Contracts complete (schema, API, UI — verified; 6E QA/docs closure done)
- **Phase 6E**: ✅ Payroll/attendance/leave/biometric spec enrichment, GRA 2026 rates verified, gy-taxcalc + v1 inspected
- **Phase 7**: ✅ Attendance + Leave complete (schema, API, UI, QA/security pass — 7A through 7H)
- **Phase 8A**: ✅ Payroll spec finalization — [payroll-implementation-plan.md](../payroll-implementation-plan.md)
- **Phase 8B**: ✅ Payroll DB schema + migration + seed (12 tables, 12 enums, GY 2026 profile)
- **Phase 8C**: ✅ Payroll calculation engine (`packages/payroll-engine/`) — 17 tests, 76 assertions
- **Phase 8D**: ✅ Payroll oRPC API — 10 router groups, ~60 procedures, PayrollInput builder, engine integration
- **Phase 8E**: ✅ Payroll settings + pay items UI — 5 routes (dashboard, settings, pay-items, loans, reimbursements), setup checklist
- **Phase 8F**: ✅ Payroll run wizard — 5-step wizard, preview generation, blockers/warnings, payslip detail
- **Phase 8G**: ✅ Payslip list/detail + print-ready layout, employee self-service
- **Phase 8H**: ✅ Payroll analytics/reports dashboard — metrics, department costs, issues, export placeholders
- **Phase 8I**: ✅ QA/RBAC/compliance pass — 9 tenant-FK security fixes, browser-verified all 8 payroll pages
- **Phase 8J**: ✅ Branding, templates, onboarding polish — checklist badges, helper copy, compliance notes, template selector
- **Phase 8K**: ✅ Payment batch + generic CSV bank export — schema, API, UI, batch lifecycle
- **Phase 8J.2**: ✅ Role string normalization + EmptyState primitive + payslip filter labels — fixes top 3 findings from the 8J.1 screenshot UX audit. Centralized RBAC helpers in `apps/web/src/lib/rbac.ts` and `packages/api/src/utils/role-helpers.ts` accept both Better Auth defaults (`owner`/`admin`) and our custom roles (`tenant_owner`/`tenant_admin`); 12+ frontend/backend files migrated off inline role arrays. `<EmptyState />` primitive shipped and applied to Pay Items / Loans / Reimbursements. Payslip segmented filters now display `Preview / Finalized / Paid` (consistent with the badge labels). HR sync chip got a tooltip and "Last HR sync" prefix. `scripts/seed-dev.ts` now promotes the org creator's membership to `tenant_owner` so future seeds match the ACL.
- **Phase 8J.1**: ✅ Payroll tabs navigation + UX clarity polish — completed in two passes:
  - First pass (commit `13f54d7`): PayrollTabs component across all 10 payroll pages, CSV injection fix (`csvCell()` helper), payment-batch state machine terminal-state guard.
  - Second pass (this phase): Overview regrouped into Payroll Work / Setup / Payments with "Recommended" highlight on the readiness-met next step; Run wizard jargon swept ("blocker" → "Needs fixing / Cannot continue", "warning" → "Needs review", raw issue codes demoted to small secondary text); Payslips list status legend (Preview / Finalized / Paid); Payslip detail negative net-pay "Needs review — blocked preview" banner and collapsible `<details>` calculation explanation; Reports "what these mean" helper text and updated export-placeholder copy; Settings reordered into General / Overtime / Work schedule / Country rules / Payslip numbering; Pay Items 6-pill filter (All / Earnings / Allowances / Deductions / Statutory / Employer Contributions); Loans pill set extended with Advances; Payments already in place per spec. Module-tabs are now a product standard — see [shared-ui-primitives-plan.md](../shared-ui-primitives-plan.md#moduletabs-pattern).

- **Phase 7I** (Leave Policy Engine / Statutory Policy Library — hardening of Phase 7 Leave; A/B/C ✅, D/E next): statutory country leave templates + effective-date versioning + tenure/probation/encashment + official source citations + adopt/snapshot/override/compare + employee "why this balance?". **7I-A** research/spec ([leave-policy-engine-plan.md](../leave-policy-engine-plan.md)): cited Guyana statutory leave — NIS maternity (13 wks @70%) + sickness (4th day, 26 wks @70%) **verified** from nis.org.gy; annual leave (Leave with Pay Act Cap. 99:02) **needs_review** (primary PDF 403; tenure bands to confirm); paternity/compassionate/unpaid **draft** (non-statutory). Correction baked in: entitlement = Labour Act/Ministry of Labour/NIS, **GRA only governs payroll/PAYE/NIS treatment** — no rule labelled "GRA". **7I-B** schema `schema/leave-policy.ts` (4 tables: leave_policy_template [org-nullable=system], leave_policy_rule [PER-RULE verificationStatus → mixed confidence], organization_leave_policy [snapshot; sourceTemplateId SET NULL; partial-unique one active per (org,country)], organization_leave_policy_rule [snapshotted, org-editable, linkedLeaveTypeId]) + 7 enums; migration **0015_futuristic_tattoo.sql** (clean additive); official-source metadata first-class. Seed `seed-leave-policy.ts` idempotent: GY (6 rules, mixed verification) + BB/TT/JM draft. **7I-C** API `routers/leave-policy.ts` (`leavePolicy`): new AC resource `leave_policy` (owner/admin/hr_admin full; payroll/manager/auditor read; employee none); 3 helpers (canManageLeavePolicy/canViewLeavePolicy/canViewLeavePayrollTreatment). templates.list/getById (payroll-treatment redacted non-finance); orgPolicies.adoptTemplate (**transactional snapshot — system edits never mutate adopted policy**)/createCustom/copyFrom/updateRule/activate(one-active-per-country CONFLICT)/archive/compareToBaseline; balanceExplanation.forSelf/forEmployee (leave_request:read, self-scoped, pending from leave_request, never blocks payroll). Unverified rules travel with every policy → UI warns; no legal certainty. Verify `verify-leave-policy-engine.ts` **22/22** (incl. snapshot invariant: system→99.00, adopted stayed 12.00). Gates: check-types 3/3, ultracite 223/1/2 unchanged, audit:permissions **86/12**. Docs: [leave-policy-engine.md](../../implementation/leave-policy-engine.md). **7I-D UI ✅** — `features/leave/{policy-labels,leave-policy-tabs}` + routes `app/leave/policies/{index,company,template/$id,org/$id}` + `app/leave/balance` + leave.css. Template gallery (verification badges + source/review), template detail (rules table + safe source link + adopt-with-snapshot-dialog), company policies list + create-custom, org policy detail (activate/archive/edit-rule/compare-to-baseline), employee "why this balance?" (available/used/pending/carry-forward + plain-language, self-scoped). Payroll-treatment redacted non-finance; every non-verified rule shows the verify-before-production notice. Browser-verified all roles (admin adopt→edit→compare→activate; auditor read-only; employee balance + no-access), 0 console errors, 8 screenshots `docs/reviews/phase-7i-d/`. Gates: check-types 3/3, build 2/2, ultracite 218/1/2 (improved), audit 86/12, web tsc 0 touched-file errors, verify 22/22. **7I-E QA + payroll-warning surfacing ✅ — PHASE 7I COMPLETE.** Added read-only `orgPolicies.health` (leave_policy:read, no new AC pair → audit stays 86/12) + `unverifiedNotice` on balanceExplanation. **Soft warnings (never block payroll, never change pay):** company policies list banner when active policy has needs_review/draft rules; employee "why this balance" unverified notice; payroll **run-preview review note** (read-only, placed under confidence not blockers — engine/formulas untouched, payroll-engine 27/27 unchanged); employee leave-request no-active-policy note. Browser-verified all roles (admin warnings + adopt/activate; auditor read-only [templates/health 200, adopt/activate/updateRule **403**]; employee balance + no-access + request form post-a11y-fix; direct-RPC RBAC: employee adopt/activate/health/templates **403**, balance.forSelf 200), **0 app console errors**, 6 screenshots `docs/reviews/phase-7i-e/`. Snapshot invariant re-confirmed (verify 22/22). **Next: return to Phase 12D Assets inventory UI.**
- **Phase 14B** (Projects + Tasks / Timelines — DB schema + migration + permissions + role + seed): ✅ doc [projects-db-setup.md](../../implementation/projects-db-setup.md). First code-touching Projects phase (no API router, no frontend routes). `packages/db/src/schema/projects.ts`: **6 tables** (project / project_member [real join table — supersedes the Horilla jsonb member-bag] / project_milestone / project_task / project_task_comment / project_time_entry) + **7 enums** (project_status, project_priority [shared by project+task], project_member_role, project_milestone_status, project_task_status, project_time_entry_status, project_linked_entity_type). Migration **0017_happy_hammerhead.sql** — purely additive (6 tables + 7 enums; **no changes to any existing table**); applied with `db:migrate`; a re-run of `db:generate` reports "No schema changes". **COORDINATION-LAYER GUARDRAILS, proven via the pg constraint catalog (26/26 DB-verify checks):** (1) `project` has **NO CRM foreign key** — `linkedCustomerId` / `linkedDealId` are plain `text` SOFT refs (the `crm_*` tables are Phase 17; CRM owns the handoff join table + back-fills the reverse link); (2) `project_task.linkedAssetId → asset` and `linkedHelpdeskRequestId → helpdesk_request` are nullable FKs **ON DELETE SET NULL** — read-only context the 14C router will only READ; (3) `project_time_entry` has **NO attendance / payroll / payslip FK** (its FKs are only project / project_task / employee_profile / user / organization) — time entries are **reporting-only**, and there is deliberately **no `payrollStatus`/lock field** (Finance owns costing/finalization later, Phase 16); (4) project **health is DERIVED at read time** (`PROJECT_HEALTH_STATES` const + type) — **no stored health column**; (5) **no `project_activity` table** — the Activity tab reuses the shared `audit_event` log; plus a generic `linkedEntityType` (enum) + `linkedEntityId` (no FK) forward-compat hook. Constraints: partial-unique `(org,reference)` + `(org,name)` for projects, `(org,reference)` for tasks, and **`(project,employee) WHERE removed_at IS NULL`** (one active membership per employee per project — 0 dups verified); tenant + status/assignee/due/link indexes on every table. FK `onDelete` discipline: employee/assignee refs **restrict** (preserve history), owned children + org **cascade**, optional + all cross-module links **set null**. **Access control** (`packages/auth/src/permissions.ts`): added resources `project` / `task` / `time_entry` to the `statement` + per-role grants (owner/admin full; hr_admin full project/task + time approve+view_costs; payroll_admin read+view_costs + time approve; manager read + task-workflow [update/assign/change_status/comment] + time approve; employee read + own-task [update/change_status/comment] + own-time [create/read/update/submit]; auditor read + view_costs + view_internal_notes; recruiter + helpdesk_agent none) + the **new `project_manager` role** (added to the `roles` map → 10 roles; full project/task control + member management + time approve, but **NOT** `view_costs`; the seed reassigns **no** existing user to it — project leads are designated via `project_member.role = 'lead'` + `project.projectManagerEmployeeId`). The `view_costs` (finance/audit) and `view_internal_notes` (PM/HR/audit) actions are the server-side **redaction gates** the 14C router enforces (assets `purchaseCost` + helpdesk internal-note precedent). **`audit:permissions` STAYS 93/13** — the new statement entries add no router-consumed pairs yet (no `projects` router until 14C), exactly the Phase-13B `ticket:approve` precedent; the count rises to ~106/14 in 14C. **Idempotent seed** `scripts/seed-projects.ts` (Atlas Shipping; ran twice → identical counts): **5 projects** (Main Office Network Upgrade [active], Vessel Crew WiFi Deployment [planning], Payroll Rollout [on_hold], HR Document Digitization [completed], Customer CPE Installation Batch [active, soft `linkedCustomerId=CUST-2026-0007` + `linkedDealId=DEAL-2026-0042`]), **11 members**, **8 milestones** (planned/in_progress/at_risk/completed/missed), **25 tasks** (all 6 statuses incl. blocked / in_review / done / cancelled; **1 asset-linked + 1 ticket-linked** task), **6 comments** (4 public / **2 internal** — for 14C redaction), **8 time entries** (draft/submitted/approved/rejected, multiple employees). The seed READS one real asset id + one real helpdesk_request id for the read-only links and **writes nothing outside `project_*`**. Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged** (the 2 drizzle-generated `0017` migration-meta files were biome-formatted to match the team convention, as the prior 16 snapshots are clean), audit:permissions **93/13 unchanged**, ultracite clean on the changed files, migration applied, seed idempotent, DB verify **26/26**. **Next: 14C Projects oRPC API** — the `projects` router (two-layer authz + lateral scope; tenant-verify every id; `budget` + internal-note server-side redaction; read-only cross-module link resolution that never writes Assets/Helpdesk; time-entry submit/approve/reject that never touches Attendance/Payroll; `PRJ-`/`TSK-` reference allocation) + `verify-projects-api.ts` + RBAC helpers byte-aligned in `role-helpers.ts` ↔ `rbac.ts`.
- **Phase 14A** (Projects + Tasks / Timelines — research & implementation plan, docs-only): ✅ plan [projects-tasks-implementation-plan.md](../projects-tasks-implementation-plan.md). **Central guardrail (user directive):** Projects is the **COORDINATION layer — link, never own** (mirrors the Helpdesk discipline): read-only nullable cross-module FKs `ON DELETE SET NULL` + generic `linkedEntityType/Id`, **zero writes** to any other module, money finance-redacted server-side. Suite group **Operations** (with Assets ✅ + Helpdesk ✅). Benchmarked Odoo/ERPNext/Zoho/MS-Project (ERP spine: people→tasks→time→cost→invoice — reserve the seam, don't build costing), OpenProject/Redmine/Plane/Taiga/GitLab (project/membership/milestone/task + simple board; defer Gantt/sprints), Linear/Asana/ClickUp/Monday/Notion/Trello (multi-view over one task table, My Tasks, plain language; defer custom fields/automations), Jira/MS-Project/ServiceNow (fixed workflow + health; defer PPM/critical-path), Basecamp (SMB altitude), Horilla (4-entity spine + member-of-timesheet rule — **supersede its jsonb member-bags with join tables**). **MVP = 6 tables**: project / project_member (join) / project_milestone / project_task / project_task_comment / project_time_entry. **Activity tab reuses shared `audit_event`** (no project_activity table). **Deferred:** task dependencies, labels, status_history, Gantt engine, templates, sprints/cycles, file attachments (no central storage), budget/cost computation. **New `project_manager` role added in 14B** (confirmed absent today — 9 roles). **AC adds resources project/task/time_entry** → audit 93/13 → ~106/14 (1 router + N distinct pairs; role addition doesn't change the count). **Coordination links** (mirror helpdesk SET NULL): `task.linkedAssetId → asset`, `task.linkedHelpdeskRequestId → helpdesk_request` (helpdesk's enum already lists `project_task` for the reverse) + generic `linkedEntityType/Id`; **CRM `project.linkedCustomerId`/`linkedDealId` = SOFT text refs, NOT FKs** (crm_* tables are Phase 17; CRM owns `crm_customer_project_link` + back-fills `projectId`). **Time entries reporting-only**; the project-time → labour-cost report is **Phase 16 Finance** (reuses `payroll-engine` + reads `contract` wageType/baseSalary + the attendance `payrollStatus` lock — **never mutates** attendance/payroll). **PMS seam:** expose task-completion; Phase 15 PMS owns the objective↔task link + bonus-point award (Projects writes nothing to PMS). **UI:** `KanbanBoard` primitive EXISTS (`apps/web/src/components/kanban-board.tsx`, @dnd-kit/core — **needs a KeyboardSensor** before reuse); `TaskChecklist` exists but onboarding-coupled (copy/genericise); **Calendar/timeline is GREENFIELD** (start with simple date-ordered milestone/task cards, NOT a Gantt). No shared ModuleTabs (copy the per-module `assets-tabs`/`helpdesk-tabs` recipe). Folder routes from day one (no flat `app/projects.tsx` stub → route-shadow). Sidebar entry budgets for the `route.tsx` a11y/lint debt (lesson #83). Standing UX rules apply: plain labels not raw enums, no raw IDs as primary text, badges carry text, loading/empty/error triad, `:focus-visible` rings (lesson #86), `aria-labelledby` on dialogs (#75), `enabled: canView` query gating. **Lifecycles:** project planning/active/on_hold/completed/cancelled/archived; task todo/in_progress/blocked/in_review/done/cancelled; milestone planned/in_progress/at_risk/completed/missed/cancelled; priority low/normal/high/urgent; **health DERIVED at read time** (on_track/at_risk/off_track/completed/no_data — never stored, like SLA). **11 RBAC helpers** byte-aligned (canView/Manage/Create/Edit/ArchiveProjects, canAssignProjectTasks, canManageProjectMembers, canTrackProjectTime, canApproveProjectTime, canViewProjectCosts, canViewProjectInternalNotes); two-layer authz (AC gate + handler scope: seesAll / member / manager-reports via `getDirectReportIds(me.id, oid)` / self); employee self-service (own task status/complete + own time log/submit; `mine`/`assignedToMe` filters). **Routes** `/app/projects/{index, all, $id, $id/tasks, $id/timeline, $id/time, my-tasks, my-time}`; detail tabs Summary/Tasks/Board/Milestones/Time/People/Activity. **Seq:** 14A spec → 14B DB+migration `0017`+role+seed → 14C oRPC `projects` API+verify → 14D overview/list → 14E detail/members/milestones → 14F tasks/Kanban/comments → 14G My-Tasks/My-Time/timesheet approve-reject → 14H simple timeline+activity → 14I QA/RBAC/security/browser pass. **Seed** (Atlas Shipping, 5 projects incl. 1 on-hold + 1 completed + 1 with CRM soft-link placeholder, milestones, ~25 tasks across all statuses, 1 asset-linked + 1 ticket-linked task, public+internal comments, time entries across draft/submitted/approved/rejected). **10 open questions answered** (add project_manager now ✓; internal-only + reserve CRM soft-refs ✓; time reporting-only ✓; no dependencies ✓; milestones separate table ✓; budget column reserved no-cost ✓; employees update-own + log-own only, creation = manage ✓; helpdesk link reserved now ✓; files deferred ✓; timeline simple cards not Gantt ✓). Gates (docs-only): check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged** (no code/schema/route/migration touched). **Next: 14B Projects DB schema (6 tables + enums) + migration 0017 + `project_manager` role + AC resources + idempotent seed.**
- **Phase 13A** (Helpdesk / Requests — spec/research, docs-only): ✅ implementation plan [helpdesk-requests-implementation-plan.md](../helpdesk-requests-implementation-plan.md). **Central guardrail (user directive):** Helpdesk is the **request/ticket LAYER that LINKS to** Assets/Payroll/Leave/Attendance/Offboarding (read-only `linkedAssetId`/`linkedPayslipId`/`linkedLeaveRequestId`/`linkedOffboardingCaseId` + generic `linkedEntityType`/`linkedEntityId`) — **never duplicates** their logic (no asset/payroll/leave mutation; pay never changes; blockers stay in `payroll_issue`, procurement in `asset_request`). Benchmarked Zendesk/Freshdesk (status + SLA-pause), Jira SM (service-request approval vs incident), Frappe/ERPNext Helpdesk (per-priority SLA, agent/team), GLPI (asset↔ticket link), ServiceNow (overkill ref), Horilla (existing extraction). **Reuses the existing `ticket` AC** (employee already holds `ticket:create` → NO new action like Assets needed; optional `ticket:approve` for approvals — open question). **MVP = 3 tables**: helpdesk_category, helpdesk_request (category/priority/status/assignee/SLA dues/single-step approval cols/read-only link cols), helpdesk_request_comment (isInternal redaction). DEFERRED: sla_policy table, status_history (pause-aware SLA), attachment (storage), approval table (multi-step), KB/canned-responses (placeholders). Status New→Open→In-progress→Waiting-on-employee/approval→Resolved→Closed(+Cancelled); priority low/normal/high/urgent; SLA states not_applicable/on_track/due_soon/overdue/breached. 10 seeded categories (HR/Payroll/Attendance/Leave/Documents/Assets/IT/Facilities/Finance/General). 7 RBAC helpers; two-layer authz; internal-note redaction. Router `helpdesk` (categories/requests/comments/approvals). UI HelpdeskTabs + /app/helpdesk{,/requests,/requests/$id,/my,/categories} (delete flat helpdesk.tsx stub — route-shadow #4; sidebar entry now safe post-12E route.tsx cleanup). Sequence 13A→13H. 8 open questions. Gates (docs-only): check-types 3/3, build, lint baseline 212 unchanged, audit 86/12. **Next: 13B Helpdesk DB schema + seed.**
- **Phase 13B** (Helpdesk / Requests — DB schema + migration + seed + permissions): ✅ doc [helpdesk-db-setup.md](../../implementation/helpdesk-db-setup.md). `packages/db/src/schema/helpdesk.ts`: **3 tables** (helpdesk_category / helpdesk_request / helpdesk_request_comment) + **5 enums** (helpdesk_request_status, helpdesk_priority, helpdesk_category_key, helpdesk_approval_status, helpdesk_linked_entity_type). Migration **0016_confused_vulture.sql** (clean additive — no existing-table column changes). **Guardrail in schema:** 6 read-only cross-module link FKs ON DELETE SET NULL (asset/payslip/payroll_run/leave_request/attendance_record/offboarding_case) + generic linkedEntityType/Id (no FK) — context/deep-link only, never mutated. **SLA state DERIVED, not stored** (`HELPDESK_SLA_STATES` const + type, computed at read time in 13C — deliberate deviation from the prompt's enum list because SLA changes with the clock, not a write; contrast assets `currentAssigneeId` which IS a write-triggered cache). Partial-unique `(org,name)` + `(org,reference)`; 6 request indexes + 2 comment indexes. requesterEmployeeId → restrict (preserve history); category/target/user/link FKs → set null; request→comment cascade. **Permissions decision (user): added dedicated `ticket:approve` action** (least-privilege — approval ≠ update; granted owner/admin/hr_admin/helpdesk_agent full, payroll_admin read+approve, manager create+read+approve, employee create+read no-approve, auditor read, recruiter none). employee already had `ticket:create` → no new self-service action (unlike assets' asset:request). **audit:permissions stays 86/12** (action in statement, no router consumes it yet — 13C will). Idempotent seed `scripts/seed-helpdesk.ts` (Atlas Shipping, verified twice → identical counts): **10 categories** (IT/Facilities/General auto-assigned to the helpdesk agent; Finance requires approval), **10 requests** (status spread incl 1 urgent+overdue; refs HD-000001..010; real linked payslip/asset/attendance/leave + generic expense link — all resolve, 0 orphans), **6 comments (4 public / 2 internal)**. DB-verified: 5 enums present, request indexes present, category uniqueness (no dup), comment visibility split 4/2, link integrity 0 orphans. Gates: check-types 3/3, build 2/2, ultracite clean on the 4 changed files, audit 86/12. **Next: 13C Helpdesk API** (oRPC router `helpdesk`; transactional reference max+1; two-layer authz: agents see queue / employees see only own via requester_employee_id; server-side internal-note redaction; derived SLA-state; approve/reject gated on `ticket:approve`; NO cross-module writes).
- **Phase 13C** (Helpdesk / Requests — oRPC API + RBAC): ✅ doc [helpdesk-api.md](../../implementation/helpdesk-api.md). `packages/api/src/routers/helpdesk.ts` registered `helpdesk` — **20 procedures** in 3 groups: `categories` (list/create/update/archive), `requests` (list/getById/createSelf/createForEmployee/update/assign/changeStatus/resolve/close/cancel/reopen/approve/rejectApproval), `comments` (list/create/createInternal). **Reuses the `ticket` AC** (no `helpdesk` resource); categories gate on `ticket:update` since `ticket` has no `manage` action. **7 RBAC helpers** in role-helpers.ts + byte-aligned rbac.ts (canManageHelpdesk / canViewHelpdesk / canAssignHelpdesk / canResolveHelpdesk / canApproveHelpdeskRequest / canViewHelpdeskInternalNotes / canCreateHelpdeskRequest); `seesAllRequests` is a local router helper (= canManageHelpdesk ∪ auditor ∪ payroll_admin; managers scoped to own + direct reports). **Two-layer authz:** the AC gate (Better-Auth `authorize` → `Missing permission: ticket:X`, coarse/role-level) PLUS a handler re-check + lateral scope (`assertRequestVisible` / direct-report / requester-self — the IDOR layer). **Server-side internal-note redaction:** `isInternal` comments are filtered out of `comments.list` AND `requests.getById` unless `canViewHelpdeskInternalNotes` (agents/HR + read-only auditor) — never the employee or a plain manager; `getById` also returns a `canViewInternalNotes` flag. **Derived SLA** (`computeSlaState`) at read time from dues + status + clock → not_applicable/on_track/due_soon/overdue/breached; per-priority `SLA_HOURS` with category `defaultSlaHours` overriding the resolution window; waiting-state pause deferred to 13G. **Reference `HD-000NNN` = MAX+1 per org with a retry loop** ((org, reference) unique is the race backstop). **Guardrail in code: ZERO writes to asset/payslip/payroll_run/leave_request/attendance_record/offboarding_case** — link ids are tenant-VERIFIED on write (SELECT-only) and resolved read-only on the detail view (only the asset name resolved; the rest returned as typed `{kind,id}` deep-link refs). Approvals gate on `ticket:approve` + `canApproveHelpdeskRequest` + manager-scope (assigned-to-me or my report; HR/payroll any); resolve requires a note, rejectApproval a reason. `scripts/verify-helpdesk-api.ts` **64/64** (categories; AC gates by role; IDOR scope; agent lifecycle; approvals; internal-note redaction; link tenant-verify + asset-NOT-mutated; derived SLA). **audit:permissions rose 86/12 → 93/13** — no router used the `ticket` resource before, so helpdesk introduces 7 `ticket` pairs + 1 router; all are in the statement, so it passes. Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit 93/13, verify 64/64, ultracite clean on changed files. **Next: 13D Helpdesk overview + request-queue UI** (HelpdeskTabs + sidebar entry; delete the flat `helpdesk.tsx` stub — route-shadow #4).
- **Phase 13D** (Helpdesk / Requests — overview + request-queue UI): ✅ review [phase-13d/](../../reviews/phase-13d/). Frontend only — no schema/API changes. New `features/helpdesk/{labels,badge,helpdesk-tabs,types}.ts(x)` + routes `app/helpdesk/{index,requests/index}.tsx` + `styles/helpdesk.css`. **Deleted the flat `apps/web/src/routes/app/helpdesk.tsx` stub** (route-shadow #4 — recruitment/onboarding/offboarding/assets precedent). Sidebar: added the "Helpdesk" NAV item (LifeBuoy icon, Operate group) + `helpdesk` to EMPLOYEE_VISIBLE_KEYS and MANAGER_VISIBLE_KEYS (helpdesk_agent already had it; recruiter NOT — canViewPayroll roles see all). `HelpdeskTabs` (Overview/Requests; null for non-viewers). **Overview** (`/app/helpdesk`): 5 status tiles (Open/In progress/Waiting on employee/Waiting on approval via `pageSize:1` count queries; Overdue/breached computed from a bounded active-queue fetch and flagged) + "Needs attention" panel (overdue / urgent / waiting-on-approval / unassigned, ≤5 each, plain badges + real requester names) + a quick link to the queue. Non-viewers branch: `canCreateHelpdeskRequest` (employee) → friendly teaser ("Your requests page is on the way" — My requests is 13F); else (recruiter) → no-access EmptyState. **Queue** (`/app/helpdesk/requests`): filterable table gated `canViewHelpdesk`. Server filters: status / priority / category / search (the API supports them). **SLA-state filter is client-side** — slaState is derived per-row, not a server filter, so it refines the current page (pager reflects the server total; documented limitation). Columns: reference, title+category, requester, status/priority/SLA badges, assignee ("Unassigned" fallback), approval (badge only when `approvalRequired`), updated date, "Linked" chip when any cross-module link is present. Reference + title are PLAIN TEXT (the `$id` detail route is 13E → no broken links). **No mutation controls** (assign/resolve/approve/internal-note) and **no internal notes** anywhere — those are 13E/13G. Plain-language label maps (no raw enums); no raw IDs as primary text; aria-labelled filters; text-bearing badges (never colour-only); list queries gated `enabled: canView` (no 403 spam). **Browser-verified 6 roles** on `:3002`: admin/owner overview + queue (10 requests); helpdesk_agent queue; **manager scoped queue = 1 (own only) vs admin's 10 — no leak**; auditor read-only (all, no mutation controls exist); employee teaser (no admin queue) + `/requests` no-access; recruiter no sidebar entry + no-access. 0 app console errors (only favicon 404), 7 screenshots in `docs/reviews/phase-13d/`. Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged**, verify-helpdesk-api **64/64**, web tsc 25 (all pre-existing — 0 in the new helpdesk files), ultracite clean on the 8 changed files. **Next: 13E request detail + comments timeline + internal-note separation + linked-entity read-only panels + status/resolve controls.**
- **Phase 13E** (Helpdesk / Requests — request detail + comments + internal notes UI): ✅ review [phase-13e/](../../reviews/phase-13e/). Frontend only — no schema/API change. New route `app/helpdesk/requests/$id.tsx` + `features/helpdesk/{comment-form,request-comments,request-linked-context,request-status-actions}.tsx`; queue title now Links to the detail (13D had it plain text); `helpdesk.css` extended (detail/summary/comments/dialog). **Detail** (`helpdesk.requests.getById`): summary grid (category / requester / [for] / assignee / logged-by / created / updated / first-response-due / resolution-due / [resolved] / [closed] / [approval]) + description + (when present) a resolution note; **read-only Linked-records panel** — only an Assets deep-link (gated `canViewAssets`), every other module a safe reference label, with "Linked for context only — changes must be made in the source module"; **no cross-module mutation affordances**; public **Conversation** timeline; **Internal notes** section rendered ONLY when `getById.canViewInternalNotes` is true; status actions. **SERVER-REDACTED internal notes — proven (the flagged guardrail):** an internal note containing `REDACTION-PROBE` was added to a request the employee owns, then the employee's own `getById` was inspected via direct RPC → `{ canViewInternalNotes: false, internalComments: 0, anyProbeText: false }` — the note is stripped server-side before it reaches the employee's browser, not DOM-hidden. Comment forms: public gated on `canCreateHelpdeskRequest` (employee/manager/agents/HR; payroll/auditor have no `ticket:create` → no form) + not terminal; internal gated on `canManageHelpdesk` (`ticket:update`) + not cancelled. Status actions: resolve (resolution note required) / close / reopen gated on `canManageHelpdesk`; cancel on `canManageHelpdesk || employee` (the requester), while active — each behind a confirm/note dialog with plain-language copy and a success toast; queries invalidated by the `helpdesk` key predicate. Detail query gated `enabled: canViewHelpdesk || canCreateHelpdeskRequest` (recruiter disabled); a non-own / non-report request → server 403 → "Request not found" EmptyState (`retry: false`, surfaces 1 intentional-probe console error). **Browser-verified 5 roles** on `:3002`: admin added a public comment + internal note (both round-trip and appear); **employee** on their own request sees the public conversation + a comment form + only a Cancel action and **no Internal notes section at all** (and the direct-RPC probe above); **auditor** read-only sees public + internal notes but has **no forms and no status actions**; **manager** → a non-report request 403s; **employee** → another's request 403s. 8 screenshots in `docs/reviews/phase-13e/`, 0 app console errors except the noted intentional 403 probes. Deviation: a manager cancelling their **own** request is not surfaced in the UI (the cancel control shows for `canManage || employee`; the server would permit an author to cancel — edge case deferred, managers act from the queue). Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged**, verify-helpdesk-api **64/64**, web tsc 25 (all pre-existing — 0 in the new helpdesk files), ultracite clean on the changed files. **Next: 13F employee My Requests self-service** (`app/helpdesk/my.tsx` — own requests list + a "Request help" create form via `requests.createSelf`, reusing the detail route; replaces the 13D teaser).
- **Phase 13H** (Helpdesk / Requests — module QA/RBAC/security/browser pass — **CLOSES Phase 13**): ✅ review [phase-13h/](../../reviews/phase-13h/). Four read-only review agents swept security/RBAC/IDOR/redaction, UI/UX/a11y/copy, integration boundaries, and API/data-consistency. **Headline results: the cross-module integration GUARDRAIL HELD (zero writes to any linked module); the security/RBAC audit found NO critical/high/medium issues; internal-note redaction is confirmed server-side in BOTH `comments.list` AND `getById`.** Small fixes applied (all sequential, re-verified): (1) `getDirectReportIds` gained an optional `organizationId` filter and helpdesk's four call sites now pass `oid` — a manager's direct-report set can no longer widen via a cross-tenant `reportingManagerId` pointer (defense-in-depth manager-IDOR hardening); (2) `assertCanDecideApproval` now throws explicitly for any non-`manager` role instead of treating "everything else" as a manager; (3) the overview page gained loading/error states so an in-flight load or error is no longer indistinguishable from a healthy empty desk; (4) `aria-labelledby` added to the two dialogs (`ConfirmDialog`, `RequestHelpDialog`) that lacked it; (5) `:focus-visible` outlines restored on search/filter/field inputs, pills, tabs, and cards (they cleared the browser default with no replacement — WCAG 2.4.7); (6) the internal-note disclaimer copy corrected to "the helpdesk team and authorized reviewers" (auditor/payroll also read internal notes); (7) a clarifying code comment for the intentional internal-note-on-terminal asymmetry. `scripts/verify-helpdesk-api.ts` expanded **79 → 96** (new section 10: cancel lifecycle + authz, every cancelled-terminal transition/comment blocked, internal note allowed on closed while public is blocked, `changeStatus` to each working state, `update` edit + terminal block, and `mine:true` self-scoping a manage-level caller). **Browser-verified 7 roles, 11 screenshots in `docs/reviews/phase-13h/`** (admin overview/queue/detail; helpdesk_agent full workflow + internal notes; payroll approve-only; manager scoped; auditor read-only; employee My-requests / detail-no-internal / queue-blocked; recruiter no-access). **Headline guardrails re-proven via the real oRPC client:** employee Rohan's own `getById` for HD-000002 → `{ canViewInternalNotes: false, internalComments: 0, anyProbeText: false }` and `comments.list` shows no internal leak; employee `getById` on a non-own request → FORBIDDEN; **manager scope after the fix #1 hardening is behavior-preserved** — a report's `getById` → OK (HD-000085), a non-report's → FORBIDDEN, queue = own + reports only. Documented + deferred (out of scope for a QA pass): reference lexical-`MAX` robustness (the `HD-NNNNNN` zero-padded format is lexical==numeric so it's safe; the `(org,reference)` unique index is the correctness backstop; the only risk is a spurious error on imported non-numeric references the system never generates), dialog focus-trap/Escape/backdrop-dismiss (an app-wide dialog-pattern enhancement, not a one-module divergence), the SLA waiting-state pause + `status_history` table, and applying the same `getDirectReportIds` org-filter to the onboarding/offboarding/recruitment callers (the param is now optional, so those paths are unchanged). Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit:permissions **93/13 unchanged**, verify-helpdesk-api **96/96**, web tsc 25 (all pre-existing — 0 in touched files), ultracite clean on the changed files. 0 app console errors except the intentional IDOR/scope probes (each one expected 403, no cascade). **Lesson #86** recorded (`outline: none` without a `:focus-visible` replacement is a recurring WCAG 2.4.7 miss — grep every module's CSS for it). **PHASE 13 (Helpdesk / Requests) COMPLETE.** Next per roadmap: **Phase 14 — Projects + Tasks.**
- **Phase 13G** (Helpdesk / Requests — assignment, SLA & approvals workflow): ✅ review [phase-13g/](../../reviews/phase-13g/). UI + **backwards-compatible API additions that all reuse existing `ticket` AC pairs, so audit stays 93/13**. **API:** `requests.list` gained `assignedToMe` (assignee = caller, resolved server-side — no client userId) and `unassigned` (NULL assignee) filters (`ticket:read`); new procs `requests.assignToMe` / `requests.unassign` (`ticket:assign`) and `requests.assignableAgents` (`ticket:assign`) — the picker returns org members in helpdesk-capable roles as `{ userId, name, role }` only (no private fields). **Approvals stay on the dedicated `ticket:approve` action, NOT `ticket:update`** (approve ≠ update — the explicit guardrail). `verify-helpdesk-api.ts` expanded **64 → 79** (section 9: assignable-agents picker + RBAC, assignToMe lifecycle, assignedToMe/unassigned filters, teammate assign, unassign, plus the employee resolve/approve/assign and manager/auditor assign negatives). **UI:** new `features/helpdesk/{assignment-controls,approval-panel,request-sla}.tsx`; `request-status-actions.tsx` extended with friendly forward transitions (Start work / Waiting on employee / Send for approval → `changeStatus`, hidden for the current status and for terminal requests); the queue gained a "Filter by assignment" select (Assigned to me / Unassigned) wired to the new server flags; employee My-requests cards show plain `employeeStatusMessage` copy ("We're reviewing this", "Waiting for your reply", …). The **detail** now renders an **Approval panel** (Approve / Reject; Reject requires a reason; gated `canApproveHelpdeskRequest`, server enforces the manager scope = report/assigned + payroll/HR any), an **Assignment panel** (Assign to me / teammate picker / Unassign; gated `canAssignHelpdesk`; hidden once terminal), and a **Service-level (SLA) panel** (derived state + human explanation + first-response/resolution due times). **SLA is honest, not paused:** the MVP clock does not subtract `waiting_on_employee` time; rather than mis-state the target, the panel says so explicitly. A status-history-backed pause is deferred (needs a new table — out of scope for 13G). **Browser-verified 6 roles on `:3002`, 11 screenshots in `docs/reviews/phase-13g/`:** admin ran the full round-trips (assign-to-me → "Sasha Bharrat", assign teammate → "Marcus James", Start work → In progress, Reject-with-reason → "Rejected by Sasha Bharrat — …"); **payroll_admin** approve round-trip → "Approved by Devon Clarke" (approve/reject only — no assignment, no status workflow, no internal notes, no comment form); **manager** approved their direct report's (Dwayne) pending finance request but a **non-report request `getById` → FORBIDDEN** and `assignToMe` is blocked (no `ticket:assign`); **auditor** read-only — sees approval state + SLA + internal notes but zero mutating affordances; **employee** sees only "Cancel request" + a public reply form + friendly status copy, and **no internal notes / assignment / approval / status-workflow controls**. **Defense-in-depth (real oRPC client):** employee `changeStatus` / `assignToMe` / `approve` / `assignableAgents` are all blocked at the AC gate (`ticket:update` / `ticket:assign` / `ticket:approve` / `ticket:assign`) while the employee public comment succeeds. 0 app console errors except the intentional scope probes. Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged**, verify-helpdesk-api **79/79**, web tsc 25 (all pre-existing — 0 in the new/changed files), ultracite clean on the 10 changed files. Deviations: queue actions are read-only (all mutations on the detail, per plan); payroll approves any category (a finance-only restriction is deferred); SLA waiting-pause deferred. **Next: 13H Helpdesk module QA/RBAC/security/browser pass** to close Phase 13. (Lesson #85: a new procedure that reuses an already-declared `(resource, action)` pair leaves `audit:permissions` unchanged — the audit counts distinct pairs + routers, not procedures.)
- **Phase 13F** (Helpdesk / Requests — employee My Requests self-service): ✅ review [phase-13f/](../../reviews/phase-13f/). Frontend + **one justified, backwards-compatible API addition**. New route `app/helpdesk/my.tsx`: a "My requests" list (filter pills Open / Waiting on me / Waiting on approval / Resolved-closed / All — client-side over one bounded `pageSize:100` fetch) + a "Request help" dialog (plain-language category picker / Summary / "Tell us what happened" / "How urgent is this?"; **no** target/assignee/internal/approval/link fields) submitting via `requests.createSelf` → success toast → navigate to the reused `$id` detail. Cards link to detail; the detail's "Back" link is now role-aware ("Back to my requests" for employees). **API:** `requests.list` gained an opt-in **`mine: true`** flag (the spec's escape hatch — the role-based list scope *blocked* a truthful self-service surface: for a manager it returned own + reports, for an HR/admin/agent it returned the **whole org**, so "My requests" would have shown the team queue / all 22 org rows). `mine` forces the strictest self-scope (requester = caller) for **any** role; it defaults off, so existing callers, the queue, and **verify-helpdesk-api stay 64/64** and **audit stays 93/13**. **Self-scope proven** via the real oRPC client signed in as the manager: `list({mine:true})` → total 3, requesters `[Andre Sealey]` (own only); `list({})` → total 7, requesters `[Dwayne Wilson, Andre Sealey]` (own + direct report — the report present in the queue is **absent** from `mine`). **HelpdeskTabs** gained a "My requests" tab shown only to viewers who can also create (managers/HR/agents); read-only auditor/payroll don't get it, and a pure employee gets the single self-service page with no tab strip. The My-requests page is gated `canViewHelpdesk || canCreateHelpdeskRequest` with `enabled`/`retry:false`, so a recruiter (no `ticket` permission at all) sees a clean "You don't have access to the helpdesk" state instead of a generic error. **Employee overview is a LANDING that LINKS to /my, not an auto-redirect:** the first attempt used a render-time `<Navigate>`, but `OrgCtx` defaults `memberRole` to `"employee"` until the active membership resolves asynchronously, so the redirect bounced **viewers/admins** to /my on first paint too (deterministic). The `EmployeeLanding` ("Your requests live here" + "Go to my requests" CTA) is correct for every role and never misroutes a viewer (lesson recorded). **Internal-note redaction re-proven for 13F:** employee Rohan's own `getById` for HD-000002 (carries the 13E `REDACTION-PROBE` internal note) → `{ canViewInternalNotes: false, internalComments: 0, anyProbeText: false }` — stripped server-side. **Server enforces self-service** (defense in depth): auditor `createSelf` → `Missing permission: ticket:create`; employee `getById` on another's request → `FORBIDDEN`. **Browser-verified 6 roles** on `:3002`, 14 screenshots in `docs/reviews/phase-13f/`: employee (list / form / filled / created-detail / public-comment-no-internal / queue-blocked / other-request-blocked / landing), admin (overview-no-redirect + My-requests tab / queue-intact 22 rows / My self-scoped), manager (My own-only), auditor (read-only, no "Request help", no My tab), recruiter (no sidebar entry + clean no-access). 0 app console errors in normal flows; the intentional 403 probes each surface one expected error (`retry:false`); a single `<Toast>` `insertBefore` error was a **test-harness artifact** (a manual `evaluate` removed Sonner's toast nodes out from under React), not a 13F defect. Deviation: the linked-context picker is intentionally omitted from the employee create form (keep self-service simple, don't surface other modules' ids); links are still shown read-only on the detail (13E) when present. Gates: check-types 3/3, build 2/2, lint **212/1/1 unchanged**, audit **93/13 unchanged**, verify-helpdesk-api **64/64**, web tsc 25 (all pre-existing — 0 in the new/changed helpdesk files), ultracite clean on the changed files. **Next: 13G** assignment + SLA + approvals workflow ("Assigned to me", approve/reject UI, pause-aware SLA via status history).
- **Phase 12E** (Assets — closes Phase 12): ✅ QA/hardening + sidebar nav + offboarding custody. **Part 1:** fixed the 6 pre-existing route.tsx lint/a11y errors the pre-commit hook blocks on (empty `catch` → commented; two menu-trigger divs → real Enter/Space keyboard handlers + justified `useSemanticElements` biome-ignore since they wrap block content; `closeAll` → useCallback) → added the **"Assets" sidebar entry** (Package icon, /app/assets). **Lint baseline 218→212.** **Part 2:** `features/assets/custody-panel.tsx` — read-only `AssetCustodyPanel({employeeId})` on the offboarding case detail (uses existing `assets.assignments.listByEmployee` — **no new API, audit stays 86/12**); shows live custody (name/category/trackingId/status/assigned/due) + EmptyState + note "Asset returns are tracked in Assets. Offboarding clearance remains manual/read-only in this version."; **NO write-back, NO mutation, NO purchaseCost**; degrades to a quiet note if unauthorized. Wired after the free-text AssetsSection (manual checklist preserved). **Part 3 QA** (browser :3002, 5 screenshots `docs/reviews/phase-12e/`, 0 app console errors): sidebar Assets navigates; employee /app/assets→My-assets redirect; custody panel verified empty + populated (no cost) + auditor read-only; manager inventory cost-column still redacted (0 leak) post-route.tsx-change; RPC RBAC re-confirmed (manager list/listMine 200, create/assign/categories.create 403). Gates: check-types 3/3, build 2/2, ultracite **212/1/1** (improved), audit 86/12, web tsc 0 new touched-file errors, verify-assets-api 46/46. **PHASE 12 (Assets) COMPLETE.** Next per roadmap: Phase 13 Helpdesk/Requests.
- **Phase 12D** (Assets — ACTIVE): ✅ Assets inventory UI. Removed the flat `assets.tsx` "Coming Soon" stub (folder-route shadow); `features/assets/{labels,assets-tabs}` + routes `app/assets/{index,inventory/index,inventory/$id,requests,my,categories}` + `styles/assets.css`. Inventory DataTable (status/category/assigned-state filters + search + pagination, plain-language status, current-holder display name, **purchaseCost column only when canViewAssetCosts**); asset detail (summary + assignment history + Assign/Return/Retire dialogs, cost redacted for non-finance, major-damage-return warning); requests queue (approve/reject-with-reason/fulfil-assign); employee **"My assets"** (own custody + my requests + request dialog + cancel, no cost); categories (create/archive). **API:** added `assets.assignments.listMine` (zero-arg self custody; reuses asset:read → audit stays **86/12**) + shared `fetchOpenCustody`. **QA bug fixed:** pickers passed pageSize:200 (schema caps 100) → 400 empty picker → fixed to 100. **Browser-verified** all roles (admin assign→return lifecycle, manager **cost-column redacted/0 leak**, employee My-assets + inventory no-access, auditor read-only) + direct-RPC RBAC (employee list/create/assign 403, listMine/createSelf 200; auditor mutations 403), **0 app console errors**, 10 screenshots `docs/reviews/phase-12d/`. Gates: check-types 3/3, build 2/2, ultracite 218/1/2 unchanged, audit 86/12, web tsc 0 touched-file errors, verify-assets-api 46/46. **Deferred to 12E:** the "Assets" sidebar nav entry — adding it edits the shared `route.tsx`, which carries pre-existing a11y/lint debt the pre-commit hook blocks on (lesson #83); that cleanup belongs in the 12E hardening pass. Module is reachable at `/app/assets` + tab-navigable. **Next: Phase 12E Assets QA/hardening** (sidebar entry + route.tsx a11y cleanup; offboarding read-only custody panel integration; requests/offboarding QA).
- **Phase 12C** (Assets — ACTIVE): ✅ Assets oRPC API. New `packages/api/src/routers/assets.ts` registered as `assets`. **Open question #1 resolved:** the `asset` AC statement had no self-service action and `employee` held only `asset:read`, so added a first-class **`asset:request`** action + granted it to owner/admin/hr_admin/payroll_admin/manager/employee/recruiter (NOT auditor/helpdesk) — `requests.createSelf`/`createForEmployee`/`cancel` gate on it (avoids the offboarding `documents.markUploaded` manage-only-gate trap). 6 RBAC helpers added byte-identically to `utils/role-helpers.ts` + `apps/web/src/lib/rbac.ts` (`canManageAssets`/`canViewAssets`/`canAssignAssets`/`canReturnAssets`/`canRequestAsset`/`canViewAssetCosts`). Router: inventory ops at root (`list`/`getById`/`create`/`update`/`retire`/`writeOff`) + sub-resources `categories` (list+assetCount/create/update/archive), `assignments` (listByAsset/listByEmployee/assign/return), `requests` (list/getById/createSelf/createForEmployee/approve/reject/fulfill/cancel). **Server-side `purchaseCost` redaction** (`redactAsset`, finance/audit only — verified over the wire: manager null, admin/auditor/payroll see it). **Transactional** assign/return via shared `performAssign`; status+currentAssigneeId mutated only there + retire (derived caches, single writer); `update` refuses them. Invariants: ≤1 open assignment (DB partial unique + re-check), assign requires available, return requires open, retire/writeOff require no open assignment, trackingId unique→CONFLICT; **major_damage return auto-retires**; history preserved. **Two-layer authz** (tenant FK verify + manager direct-report / employee-self lateral scope). Offboarding integration is read-only (`assignments.listByEmployee`); no write-back. **Verify:** `scripts/verify-assets-api.ts` 46/46 (listing+redaction, IDOR, assign/return lifecycle+invariants, lateral scope, requests workflow, write RBAC). Gates: check-types 3/3, build ✓, ultracite 223/1/2 unchanged, audit:permissions **80/11**, changed-files lint clean. Docs: [assets-api.md](../../implementation/assets-api.md). **Next: Phase 12D Assets inventory UI.**
- **Phase 12B** (Assets — ACTIVE): ✅ Assets DB schema + migration + seed (DB layer only; no router/UI yet). `packages/db/src/schema/assets.ts` — 4 tables (asset_category, asset, asset_assignment, asset_request) + 3 enums (asset_status, asset_return_condition, asset_request_status), org-scoped, soft-delete, registered in schema barrel. Migration **`0014_fat_shiver_man.sql`** (clean diff, no drift) applied. Three partial-unique invariants: `asset_org_tracking_uq` (tag/serial per org), `asset_category_org_name_uq`, and the core **`asset_assignment_open_uq`** (≤1 open assignment per asset). `asset.currentAssigneeId` is a denormalised cache of the open assignment (12C assign/return keep it in sync). `purchaseCost` numeric(12,2) — future 12C API must redact for non-finance roles. `scripts/seed-assets.ts` (idempotent, delete-then-insert): 5 categories, 10 assets (across available/in_use/retired incl. maintenance + write-off), 8 assignments (5 open + 3 returned: healthy/minor/major), 4 requests (one per status). **Verified:** tables/enums/indexes exist; seed idempotent (identical counts ×2); one-open-assignment + trackingId-unique invariants both enforced (insert→reject); org-scoped. Gates: check-types 3/3, build ✓, ultracite 223/1/2 unchanged, audit:permissions 73/10 (no router added). Docs: [assets-db-setup.md](../../implementation/assets-db-setup.md). **Offboarding:** no coupling added this phase (live-custody read-only link comes in 12C/12D). **Next: Phase 12C Assets oRPC API.**
- **Phase 11H** (closes Phase 11): ✅ Module-wide QA/RBAC/security/browser pass. Three parallel READ-ONLY audits (security/RBAC, UX/a11y, integration/correctness) then sequential fixes for confirmed defects only. **Fixes (8):** payslips list/run-preview showed a raw cuid as the employee "name" → `payslipsList` now leftJoins `employeeProfile` (name + via `getTableColumns`); raw `wageType` enum → `wageTypeLabel` ("Monthly salary"/"Daily rate"/"Hourly rate") in run + payslips; raw correction status enum → `correctionStatusLabel`; biometrics overview "Geofencing (coming soon)" `<a>` (full reload, wrong label) → `<Link to="/app/geofencing">Geofencing`; `containsBiometricTemplate` now **recurses** into nested objects/arrays (a template under `meta.fingerprintData` was previously accepted by `.passthrough()`); public `ingest.submit` now rejects punches from non-`active` devices (was id+key+deletedAt only); `buildExceptionReview` period scoping now compares on **date granularity** (date-mode period bounds vs timestamp `when` dropped last-day exceptions after midnight — see lessons #80). **Documented (hardening backlog, not changed in a QA pass):** manager visibility of org-wide device mappings / geofence assignments (A1/A2 — `canViewBiometrics` intentionally grants managers device-view; low); processor `applyInPunch`/`applyOutPunch` non-transactional partial-failure double-create window (C-D1 — wrap in `db.transaction`); unmapped punches (employeeId null) surface in the org exception queue but not per-employee readiness (C-D2); `duplicate_punch` exception type is dead (duplicates dropped at ingest by idempotencyKey — C-D3); `attendance_event` GPS scrub uses date-granularity cutoff (C-D5, privacy-safe over-scrub ≤1 day). **RBAC matrix (live):** owner/admin/hr manage all; payroll_admin devices/geofence create **403**, payroll view ✓; manager device/geofence **403**, scoped punch/exception view, run.confirm **403**; auditor read-only (all mutations **403**, no action buttons — "Resolved/Dismissed" are filter chips); employee global punches/exceptions **403**, own projection 200 no resolutionLink, blocked-page no-access state; public ingest bad key **401**, nested template rejected, inactive device **403**. **Security/privacy:** no template columns/material stored or accepted (recursion-proven); apiKeyHash/credentialRef never returned; no raw GPS to employees; no raw enums/IDs as primary UI; planned adapters (ZKTeco TCP/ADMS, NGTeco cloud) clearly "Planned" with no fake Sync. **Scripts:** verify-work-arrangement ✓, verify-biometric-payroll-readiness ✓, verify-live-pay-projection ✓, verify-biometric-geofence-attendance-payroll-integration 22/22 ✓, scrub dry-run 0-eligible ✓. Gates: check-types 3/3, payroll-engine 27/27, build ✓, web tsc 26 baseline (0 new; touched-file matches are pre-existing baseline lines), ultracite 223/1/2 unchanged, audit:permissions 73/10. 12 screenshots `docs/reviews/phase-11h/`, 0 app console errors. **PHASE 11 COMPLETE. Next: Phase 12 Assets.**
- **Phase 11G CP4** (closes 11G): ✅ GPS retention scrub + attendance source labels + full-chain integration verification. Privacy hardening, **no migration** (schema already had nullable `geofence_check_in.lat/lon` + `coordsPurgedAt` + `attendance_setting.gps_retention_days` default 90), no new sync protocol, no tax/rate change. **Scrub** (`scripts/scrub-geofence-gps.ts`): after the per-org retention window, NULL precise `geofence_check_in.latitude/longitude` + stamp `coordsPurgedAt` (KEEP verdict/distance/accuracy/matchedWorkSite/reason/capturedAt) and NULL `attendance_event.locationLat/Lon`; DRY-RUN by default (`--apply` to write, `--org=` filter); recent rows untouched; prints eligible/skipped-recent/already-scrubbed counts. **Source labels** (`attendance.recordsList` → `enrichRecordsSourceReview`): each daily record enriched with a `source` key (from linked `attendance_event` sources: single→that, multiple→`mixed`, none→`none`) + `needsReview` flag (open/in_review exception on employee+date); UI maps the key → plain "Manual entry/Biometric device/Mobile GPS check-in/File import/Admin adjustment/Mixed sources/Source unavailable" + "Needs review" badge (no raw enum/ID, no raw GPS in payload). **Verified**: `scripts/verify-biometric-geofence-attendance-payroll-integration.ts` 22/22 (raw-punch safety, exceptions, readiness, projection, privacy/scrub, processor link+idempotency — read-only assertions before the mutating processor run, see lessons-learned #79); scrub script proof (dry-run no-op → apply scrubs synthetic 200-day row, coords null + coordsPurgedAt set + audit fields preserved, recent row untouched); existing verifiers still pass. Browser: attendance Source column + labels (Devon=Mobile GPS, Maya/Rohan=Biometric, Kareena=Mobile after processing) + Needs-review badges, no lat/lon in payload, estimated-pay card still renders, geofence check-in renders without coords, 0 console errors. RBAC: employee → biometric exceptions/punches **403** + own projection no resolutionLink; auditor → attendance read 200 / validate **403**. Gates: check-types 3/3, payroll-engine 27/27, build ✓, web tsc 26 baseline, ultracite 223/1/2 unchanged, audit:permissions 73/10. Screenshots `docs/reviews/phase-11g-cp4/`. **Phase 11G COMPLETE. Next: Phase 11H — module-wide QA/RBAC/security/browser pass.**
- **Phase 11G CP3** (ACTIVE): ✅ Live / projected pay hardening — projected pay is an **estimate** (never finalization) that now expresses a **confidence level** driven by the CP2 attendance/exception signals, so an estimate never looks more trustworthy than its data. No tax/rate formula change. Engine: new `confidence.ts` (single source of truth — the confidence logic was duplicated in `calculate.ts` + `projected-pay.ts` and had gone stale w.r.t. CP2 exception fields; unified). `deriveConfidence`: `cannot_estimate` (blockers) → `low` (openExceptionWarnings/unprocessedPunches/pendingItems/pendingLeave) → `medium` (incomplete) → `high`; `confidenceLabel` → **High confidence** / **Needs review** / **Cannot finalize yet**; `buildConfidenceReasons` plain-language. `calculateProjectedPay` returns richer `ProjectedPayResult` (confidenceLabel, confidenceReasons[], warnings[], blockers[], payType/payFrequency, hours{regular,overtime}, days{worked,absent,paidLeave,unpaidLeave}, attendanceComplete); `disclaimers[0]` always the not-final guardrail; **paid leave never reduces** monthly base, unpaid does (unchanged). `calculatedAt` stamped by API (engine stays Date-free). API: `projectedPay.forEmployee` (admin) + `.own` (self-scoped) — `payPeriodId` now **optional** (defaults to current/open period via `resolveProjectionPeriod`); `.own` **strips `resolutionLink`** (no admin links to employees); `runs.preview` returns `confidenceCounts{high,needsReview,cannotFinalize}`. UI: employee **"Estimated pay this period"** card (`features/payroll/estimated-pay-card.tsx`) on `/app/payroll/payslips`; admin **"Projected pay confidence"** indicator in run-preview review step. **Verified**: `scripts/verify-live-pay-projection.ts` 13/13 + `projected-pay.test.ts` 9 cases (engine 27/27); browser — employee card "Cannot finalize yet" (Rohan, open exception) + "Needs review" (Andre, unvalidated only), admin preview "0 High · 7 Needs review · 3 Cannot finalize yet", own payload has NO resolutionLink (leak-prevention), auditor `forEmployee` 403, 0 console errors. Hourly/daily projection covered by unit tests (no hourly/daily contract seeded — limitation logged; "shift" = daily, no enum); true "High confidence" not reachable in seed (every employee has unvalidated attendance) — documented not faked. Gates: check-types 3/3, payroll-engine 27/27, build ✓, web tsc 26 baseline, ultracite 223/1/2 unchanged, audit:permissions 73/10. Screenshots `docs/reviews/phase-11g-cp3/`. **Next: CP4 GPS retention scrub + final integration verification + attendance source labels.**
- **Phase 11G CP2** (ACTIVE): ✅ Payroll readiness from attendance exceptions — open biometric/geofence/attendance exceptions now surface as payroll **blockers/warnings** so they can never silently reduce or inflate pay. Backend-first, **zero** payroll calc/tax change. Builder (`payroll-input-builder.ts`): `buildExceptionReview` period-scopes each open/`in_review` exception via its linked punch (`punchTime`) → event (`eventDate`) → geofence check-in (`capturedAt`) → `createdAt` fallback, tallies by severity into `openExceptionBlockers`/`openExceptionWarnings` (+ `exceptionSummary` plain labels + `unprocessedPunches` = pending/error punches in period); `buildPayrollInput` plumbs `flags.blockPayrollOnOpenExceptions` from `attendance_setting.block_payroll_on_open_exceptions` (default **true**). Engine (`blockers.ts`, pure): emits `UNRESOLVED_ATTENDANCE_EXCEPTION` (blocker, when policy on) / `ATTENDANCE_EXCEPTION_REVIEW` (warning; **downgrades** blockers when policy off — never disappears) / `UNPROCESSED_PUNCHES_FOR_PERIOD` (warning). `runs.confirm` now gates on a **live count of open blocker `payroll_issue` rows** (not cached `blockerCount`) so override/resolve is reflected instantly. UI: plain-language labels in `ISSUE_CODE_LABELS` + preview note "Raw device punches do not go directly to payroll; payroll uses processed attendance after review." Worked minutes always read from `attendance_record` (raw-punches-never-paid holds). **Verified**: `scripts/verify-biometric-payroll-readiness.ts` 11/11 (blocker, policy-off downgrade, warning, unprocessed, resolve-unblocks-then-restore); browser as owner on May 2026 Payroll preview — blocker "Unresolved attendance exception — Rohan Gopaul … (missing clock-out)" under "cannot continue", warnings for Maya (GPS accuracy) + unprocessed punches, persisted issues `UNRESOLVED_ATTENDANCE_EXCEPTION`×1/`ATTENDANCE_EXCEPTION_REVIEW`×1/`UNPROCESSED_PUNCHES_FOR_PERIOD`×5, `runs.confirm` → **412 "Cannot confirm: 5 unresolved blocker(s)"**, 0 console errors (throwaway preview run cleaned up). RBAC: employee + auditor → run page denied (auditor `runs.confirm` 403 `payroll:update`), payroll_admin + owner → allowed; no raw GPS/payloads/secrets on any payroll surface. Gates: check-types 3/3, payroll-engine 18/18, web build ✓, web tsc 26 baseline, ultracite 223/1/2 unchanged, audit:permissions 73/10. Screenshots `docs/reviews/phase-11g-cp2/`. **Next: CP3 live pay projection hardening (hourly/daily/shift/salaried + confidence levels + employee "Estimated pay this period").**
- **Phase 11G CP1** (ACTIVE): ✅ Work-arrangement modes — remote/hybrid/field/exempt workers are no longer flagged "outside geofence" (the payroll-safety prerequisite for CP2). Migration **`0013`**: `work_arrangement` enum (onsite/hybrid/remote/field/exempt) + additive `employee_work_info.work_arrangement` column (default onsite). `utils/geofence.ts`: `resolveEmployeeArrangement` + `arrangementPolicy` (only **onsite** enforces the fence + raises exceptions + requires GPS; hybrid is fence-aware but never auto-exceptions; remote/field/exempt = no fence, no exception, GPS optional). `checkIns.createSelf`/`previewSelf` made arrangement-aware: GPS now optional for non-onsite; **outside_geofence / low_gps_accuracy exceptions only fire for onsite** (so remote workers never block payroll); previewSelf returns arrangement/label/geofenceEnforced/gpsRequired. Mobile check-in UI: friendly **"Remote work check-in"** framing + optional WFH/site note + GPS-optional ("No location captured" allowed) for non-onsite — never the scary "away from work location" for remote. `insertCheckInExceptions` + `computeVerdictView` extracted to hold the complexity ceiling. Seed: kareena→remote, devon→field. **Verified**: `scripts/verify-work-arrangement.ts` (policy classification for all 5 modes + DB resolution), and browser as a remote employee — "Remote work check-in" shown, GPS-denied still allowed, submit created a check-in with status `unverified` and **NO outside_geofence exception** (server-confirmed); employee 403s on the exception queue (RBAC). Gates: check-types 3/3, web build ✓, web tsc 26 baseline (0 biometric), ultracite 223/1/2 unchanged, audit:permissions 73/10. Screenshots `docs/reviews/phase-11g/`. **Next: CP2 payroll readiness blockers/warnings from open exceptions.**
- **Phase 11F**: ✅ Geofence locations + employee mobile check-in UI — `apps/web/src/routes/app/geofencing/{index,locations/index,locations/$id,check-in}.tsx` + `apps/web/src/features/biometrics/{geofencing-tabs,mobile-check-in,geofence-location-form-dialog}.tsx` + geofence labels; deleted flat `routes/app/geofencing.tsx` stub. Small API add: **`biometric.checkIns.previewSelf`** (read-only verdict preview — runs the SAME server-side resolveWorkSiteForEmployee+evaluateCheckIn as createSelf, writes nothing; reuses the existing `geofence:check_in` action so audit stays 73/10). **Overview** `/app/geofencing`: dashboard (Active locations / Assignments / Outside / Low-accuracy / Open exceptions cards + attention: outside-review/low-accuracy/unassigned-locations) for canViewGeofencing; employees fall through to the mobile check-in screen. **Locations** `/app/geofencing/locations`: cards with plain-language "Allowed radius: 150 m" / "GPS accuracy required: 100 m or better", assignment counts, New-location (canManage); **detail** `/app/geofencing/locations/$id`: rules + assignments + Edit/Archive + technical coords (admin-only section); create/edit via `GeofenceLocationFormDialog` (lat/lon/radius/accuracy validation, "map picker coming later" note). **Mobile check-in** `/app/geofencing/check-in` (the centerpiece, mobile-first, non-technical wording): "Get my location" → `navigator.geolocation.getCurrentPosition` (tap-only, no background/watchPosition) → `previewSelf` → worker-friendly verdict ("You're at your work location" / "away from…" / "signal is weak" / "couldn't confirm") + site name + allowed radius → Clock in / Clock out; outside → reason required (submit disabled until entered); permission-denied/unavailable/timeout → distinct friendly recovery copy; privacy note (no background tracking, no biometric templates) + payroll-safety note ("may need review before pay"); recent check-ins list (status labels only, NO raw coords). Server decides the verdict (client never trusted). **RBAC UX**: canManageGeofencing → manage locations; canViewGeofencing (HR/manager/auditor/payroll) → view + tabs; canUseGeofenceCheckIn (incl. employee) → mobile check-in; auditor read-only; employee no location-management access. Privacy: no raw GPS coords shown to workers; no continuous tracking; no templates/secrets/payloads. **Browser-verified** :3002: admin overview/locations/create-dialog; **employee inside check-in (created, status inside), outside check-in with reason (created, status outside + exception), permission-denied friendly error, blocked from location management**; auditor read-only (no New button); 0 app console errors. 9 screenshots `docs/reviews/phase-11f/`. Reseeded clean after the check-in mutations. **Gotcha (lesson #76)**: restarted the API server so `previewSelf` mounted. Gates: check-types 3/3, web build ✓, web tsc 26 baseline (0 geofencing), ultracite 223/1/2 unchanged (fixed 1 nested-ternary), audit:permissions 73/10 PASS. **Next: 11G attendance/payroll integration hardening.**
- **Phase 11E**: ✅ Punch review + attendance exception queue UI — `apps/web/src/routes/app/biometrics/{punches,exceptions}.tsx` + `apps/web/src/features/biometrics/{map-device-user-dialog,exception-action-dialog}.tsx` + labels/tabs additions; small API enrich (`biometric.exceptions.list` now returns `resolutionNote`/`attendancePunchId`/`geofenceCheckInId`). **Punch review** (`/app/biometrics/punches`): "Punch review" title, payroll-safety note ("Raw punches do not go directly to payroll…"), filters (All/Pending/Processed/Unmapped/Duplicate/Error), enriched table (employee/device-user, device name, direction, method, source, status + error reason), **Map** action on unmapped rows → `MapDeviceUserDialog` (employee picker via hrCore.employees.list, `mappings.create`, "run processing again" guidance), **Process pending** → confirm dialog ("turns staged punches into attendance events… does not finalize or update payroll"). **Exception queue** (`/app/biometrics/exceptions`, new BiometricTabs "Exceptions" tab): filters (All open/Blockers/Warnings/Info/In review/Resolved/Dismissed), table (employee/type/source[derived from type]/severity[not color-only: "Blocker — payroll cannot continue"]/status/detail + resolution note when closed), **Acknowledge** (→in_review, no note) / **Resolve** / **Dismiss** (note required via `ExceptionActionDialog`). RBAC UX: canManageBiometrics → Process/Map; canReviewAttendanceExceptions (HR+manager) → ack/resolve/dismiss; auditor read-only; payroll/manager view; employee no-access. Privacy: no raw payloads/templates/secrets/GPS coords; no internal IDs as primary text. **Browser-verified** admin (punch review + map dialog populated + process confirm + queue + **resolve round-trip 3→2 open with note persisted**), auditor (read-only, no action buttons), employee (no-access, no leak); 0 app console errors after fixing a bug. 7 screenshots `docs/reviews/phase-11e/`. **Bug found+fixed:** the map dialog called `hrCore.employees.list` with `pageSize:200` → 400 (schema max 100) → fixed to 100. **Gotcha (lesson #76 addendum):** `--hot` didn't reload the `exceptions.list` select change → restarted the API server (cwd/PID-confirmed). Gates: check-types 3/3, web build ✓, web tsc 26 baseline (0 biometric), ultracite 223/1/2 unchanged, audit:permissions 73/10 PASS. **Next: 11F geofence locations + employee mobile check-in UI.**
- **Phase 11D**: ✅ Biometric devices UI (first UI checkpoint) — `apps/web/src/features/biometrics/{biometric-tabs,biometric-ui,labels,import-punches-dialog}.tsx` + `apps/web/src/routes/app/biometrics/{index,devices/index,devices/$id,sync-runs,punches}.tsx` + `styles/biometrics.css`. Deleted the flat `routes/app/biometrics.tsx` stub (folder-route shadow). **Overview**: "Biometrics & time clocks" + 5 stat cards (active devices / sync runs / pending punches / open exceptions / planned adapters) + attention panel (unmapped / failed-partial runs / pending / open exceptions / planned-only devices) + quick links (incl. Geofencing coming-soon). **Devices list**: 4 seeded vendor devices as cards with vendor/model/family, **Supported now / Planned badge**, connection mode, capacity, punch-method + connectivity tags. **Device detail**: header + Connection&adapter (honest "Live sync: Planned — not available yet" for ZKTeco TCP/ADMS; "Not used (file import)" for NGTeco) + vendor guidance ("How to get punches") + Capabilities + Employee mappings + Recent sync runs + Recent punches + Security&privacy notes (no templates stored / secrets never shown / planned ≠ live). **Import punches** dialog (CSV paste, process checkbox, honest "file upload not available yet") gated to canManage. **Punches** list (status filter pills + "Process pending" for canManage). **Sync runs** list. Friendly labels via `features/biometrics/labels.ts` (mirrors server `providerKeyForDevice` for the supported/planned derivation). Privacy: apiKeyHash/credentialRef never rendered (server strips them; UI only shows `hasIngestKey`). RBAC UX: owner/admin/hr manage+import; auditor read-only (no Import/Process); payroll/manager view; employee no-access. **Browser-verified** on :3002 (admin/auditor/employee): all routes load, planned states honest (no fake live sync/Sync-now buttons), NGTeco shows export path not fake cloud API, auditor sees no mutation controls, employee blocked with no data leak; **0 app console errors** (only favicon 404). 7 screenshots in `docs/reviews/phase-11d/`. **Gotcha re-hit (lesson #76):** the long-running `bun --hot` API server predated the 11C `biometric` router → `/rpc/biometric/*` 404'd until I restarted the Heimdallone API server (cwd/PID-confirmed; Netsurf/StockHub untouched). Gates: check-types 3/3, web build ✓, web tsc 26 baseline (0 biometric), ultracite 223/1/2 unchanged, audit:permissions 73/10 PASS. **Next: 11E punch review + exception queue UI.**
- **Phase 11C**: ✅ Biometric + Geofencing sync/import API + punch processor — `packages/api/src/routers/biometric.ts` (`biometric` router: devices / mappings / syncRuns / punches / processor / ingest / geofences / assignments / checkIns / exceptions). **Multi-vendor adapter/provider model** (`utils/attendance-adapters.ts`): `AttendanceDeviceAdapter` interface + registry — `generic_csv/excel/api` + `ngteco_app/kseries` supported; `zkteco_tcp`, `zkteco_adms`, `ngteco_cloud` **planned** (no faked live sync); migration **`0012_clever_wrecker.sql`** added `attendance_vendor` enum, expanded `attendance_device_mode` (csv/excel/usb/api + zkteco_tcp_planned/zkteco_adms_push_planned/ngteco_cloud_export/ngteco_app_export/vendor_manual_upload/custom_adapter_planned), and device capability columns. **Source-agnostic processor** (`utils/biometric-processor.ts`): pending/unmapped punch → map → dedupe → `attendance_event`(source) → `recalculateRecord` (extracted to `utils/attendance-recalc.ts`, shared with the attendance router) → exceptions (unmapped/missing-clock-out/clock-drift/out-of-window); idempotent (no duplicate events on re-run). **Geofence** (`utils/geofence.ts`): great-circle distance + employee→department→org work-site resolution + server-side evaluate; `checkIns.createSelf` is self-scoped (`geofence:check_in`, employees hold it — not manage-gated), soft-blocks outside-with-reason. **Public `ingest.submit`** authenticates by deviceId + ingest API-key hash (org derived from device), rejects biometric-template payloads, dedups by idempotency key. 4 new AC resources (attendance_device/attendance_punch/geofence/attendance_exception) + 6 RBAC helpers (canManage/View Biometrics, canManage/View Geofencing, canUseGeofenceCheckIn, canReviewAttendanceExceptions). **Privacy:** no templates ingested/stored (verify_mode = method only); device secrets (apiKeyHash/credentialRef) never returned. Seed updated to 4 vendor devices. Gates: check-types 3/3, web build ✓, ultracite **224/1/2 unchanged**, **audit:permissions 73/10 PASS** (was 62/9). Verified via `scripts/verify-biometric-api.ts` (adapter parse + geofence math + privacy guard + processor end-to-end + idempotency). See [biometric-geofencing-api.md](../../implementation/biometric-geofencing-api.md). **Next: 11D devices UI.**
- **Phase 11B**: ✅ Biometric + Geofencing DB schema + migration + seed — `packages/db/src/schema/biometric.ts` (**8 tables**: geofence_location, attendance_device, attendance_device_employee_map, attendance_device_sync_run, attendance_punch [raw staging], geofence_assignment, geofence_check_in, attendance_exception; **13 enums**) + `attendance_setting` extended with 6 additive policy columns (enable_geofenced_check_in / default radius 150m / accuracy 100m / clock_drift_threshold 300s / gps_retention 90d / block_payroll_on_open_exceptions). Migration **`0011_early_lady_vermin.sql`** applied to postgres-central (13 enums, 8 tables, 6 ALTER ADD COLUMN, **0 destructive**). `scripts/seed-biometric.ts` (re-runnable): 2 sites, 2 devices (api_ingest + csv_import), 5 device-user maps (1 intentionally unmapped), 3 geofence assignments, 2 sync runs (success + partial), 8 punches (normal/duplicate/missing-out/error/unmapped/2 mobile), 3 check-ins (inside/outside-with-reason/low-accuracy), 5 exceptions (blocker×2/warning×2/info×1; open/resolved/dismissed). Key invariants as partial-unique indexes: punch idempotency `att_punch_idem_uq` (NULL-safe single key — **negative-tested: duplicate rejected**), `att_dev_map_device_user_uq`, device serial, site name. **Privacy verified in-seed**: no biometric-template columns exist (information_schema introspection), no plaintext secret seeded (`credential_ref` null, `api_key_hash` is a hash). Reuses attendance_event/record/correction (no fork). Gates: check-types 3/3, web build ✓, ultracite 224/1/2 baseline unchanged, audit:permissions 62/9 PASS (new AC resources land in 11C). See [biometric-geofencing-db-setup.md](../../implementation/biometric-geofencing-db-setup.md). **Next: 11C — device sync/import API + punch processor.**
- **Phase 11A**: ✅ Biometric + Geofencing implementation plan — [biometric-geofencing-implementation-plan.md](../biometric-geofencing-implementation-plan.md). Spec/research A-phase only (no code/schema). Live-researched Odoo `hr_attendance`/kiosk, ERPNext `Employee Checkin` + Shift-Type auto-attendance + `biometric-attendance-sync-tool`, Horilla attendance/biometric/geofencing, OpenHRMS/Cybrosys, and ZKTeco `pyzk`/ADMS — synthesised against the **existing** Phase 7 Attendance module (which already has `attendance_event.source` incl. `biometric`/`mobile`/`import`, `deviceId`, `locationLat/Lon` stubs; `attendance_record` payroll-gated by `payrollStatus`; `attendance_correction` review flow). Decided minimum entity set: **8 new tables** (`attendance_device`, `attendance_device_employee_map`, `attendance_device_sync_run`, `attendance_punch` raw staging, `geofence_location`, `geofence_assignment`, `geofence_check_in`, `attendance_exception` = the review queue) + 13 new enums; **reuses** attendance_event/record/correction/setting (no fork). Core model: raw punches **staged, never authoritative** → map→dedupe→geofence→processor → `attendance_event`(source=biometric/mobile/import) → `recalculateRecord` → `attendance_record`; **only approved records reach payroll**. Device protocol lives in an **external sync-agent + punch-ingest REST API + CSV import** (Bun can't speak the ZK binary protocol — Frappe's model); native TCP pull + ADMS push deferred. Geofence = **soft-block-with-reason** (server-side great-circle), GPS retained 90d then scrubbed, **never store biometric templates** (verifyMode = method enum only). New AC resources `attendance_device`/`attendance_punch`/`geofence`/`attendance_exception` + helpers canManageBiometrics/canViewBiometrics/canManageGeofencing/canUseGeofenceCheckIn/canReviewAttendanceExceptions; mobile check-in stays a `tenantProcedure` self-scope (avoids the documents.markUploaded manage-gate trap). UI routes `/app/biometrics{,/devices,/devices/$id,/sync-runs,/punches}` + `/app/geofencing{,/locations,/check-in}` + exceptions/corrections integrated into Attendance. Sequence **11A–11H** (B schema+seed → C sync/import API+processor → D devices UI → E punch/exception queue → F geofence+mobile check-in → G attendance/payroll integration → H QA/RBAC/security). Next: **11B Biometric + Geofencing DB schema + migration + seed.**
- **Assets (queued spec — Phase 12 candidate, drafted 2026-06-02)**: ✅ Implementation plan — [assets-implementation-plan.md](../assets-implementation-plan.md). **Not the active phase** (Phase 11 is Biometric + Geofencing per the roadmap correction above); this spec is drafted and queued, no DB migration to be run yet. Synthesised from `modules/assets-spec.md` + `horilla-extraction/assets.md` into 4 Drizzle tables (asset_category, asset, asset_assignment, asset_request) + 3 enums (asset_status, asset_return_condition, asset_request_status); the `assets` oRPC router (categories / assets / assignments / requests) with the two-layer authz (tenant + manager direct-report scope), `purchaseCost` redaction for non-finance roles, transactional assign/return keeping `asset.status`+`currentAssigneeId` in sync, and the **one-open-assignment partial unique** invariant; canManageAssets/canViewAssets/canRequestAsset helpers to mirror the existing `asset` AC grants; UI checkpoints CP1 inventory → CP2 detail+history → CP3 assign/return → CP4 requests+self-service → CP5 integration+QA; and a **read-only** offboarding custody panel (no auto write-back to the free-text asset_return rows in v1). Bakes in the 10D lessons up-front: folder routes (no shadow), graceful 403/404 + retry:false, denormalised display fields in list endpoints, and a flagged open question to add an `asset:request` AC action rather than ship a self-service branch behind a manage-only gate (the offboarding `documents.markUploaded` trap). **Queued behind Biometric + Geofencing (Phase 11)** — its B/C/D/E checkpoints resume when Assets becomes the active phase (Phase 12 candidate).
- **Phase 10D CP6**: ✅ Offboarding QA / RBAC / browser pass (closes Phase 10D) — verification + hardening pass, no new features. Two parallel READ-ONLY review agents audited the UI (static QA/a11y) and the API (authorization correctness + role×capability matrix). **Confirmed clean:** frontend `rbac.ts` mirrors backend `role-helpers.ts` byte-for-byte for the three offboarding helpers; the 10C `assertCaseVisibleToCaller` manager-scope (IDOR) fix covers all 9 manager-reachable per-case reads + `cases.approve`; `redactCase`/`interviewGetByCaseId` redaction matches spec; no raw enums as primary text, no internal IDs as primary text, no unsafe hrefs, every input labelled, every dialog has role/aria-modal/aria-labelledby. **Fixed (sequential, me):** (1) the read-only case detail (`cases/$id.tsx`) now shows a graceful "Case not available" EmptyState when `getById` is forbidden/404 instead of a blank page; the per-case list queries (tasks/assets/access/docs) are gated on the case being visible (`enabled: Boolean(c)`) so an IDOR-blocked manager no longer triggers a cascade of 403s; extracted `useCaseDetailData` hook to keep `CaseDetail` ≤ Biome complexity 20; (2) `MyOffboarding` shows a graceful "Not available" state when `getMyCase` 403s (roles without `resignation:read`, e.g. recruiter) instead of an unusable resignation form; (3) `retry: false` on `getById`/`getMyCase` so the no-access state appears immediately (~1s vs ~7s) and the 403 console noise drops to a single request. Browser-verified the full RBAC matrix on :3002: owner full actions, auditor read-only + settlement, employee self-service, **manager** scoped list (0 rows for no reports) + IDOR-blocked case → "Case not available" with NO data leak, **recruiter** → "Not available", **payroll_admin** → views case + settlement data, NO manage buttons, `internalNote` redacted. **Documented as 10-series hardening backlog (not fixed — out of QA-pass scope):** (a) app-wide a11y — section titles use `div.eyebrow` not `<h2>` (the established design-system pattern across every module; changing only offboarding would break consistency); (b) latent 10C API bug — `documents.markUploaded` has an employee self-service branch that is unreachable because its AC action is `manage_documents` (employees don't hold it); fails safe (no over-permission) and no UI exposes employee document upload, so zero user impact today; (c) managers cannot approve/reject a direct report's pending resignation via the UI (`CaseStatusActions` is gated to `canManage`=HR), though the API allows it. Gates: build 0, web tsc 0 offboarding errors (26 baseline), lint 224/1/2 unchanged. Screenshots: `docs/reviews/phase-10d-cp6/`.
- **Phase 10D CP5**: ✅ Offboarding employee self-service — `apps/web/src/features/offboarding/my-offboarding.tsx` (`MyOffboarding`) replaces the CP1 "coming later" placeholder; wired into `routes/app/offboarding/index.tsx` (employee fall-through) and the `routes/app/offboarding/my.tsx` route. The employee surface is intentionally narrow because the offboarding `*.list` endpoints are HR-gated (`canViewOffboarding` excludes employees) — clearance (asset return / access revocation / settlement) is an HR/IT responsibility, not the departing employee's. So the page uses only the three resignation-resource procedures the employee role is granted (`resignation: create/read/withdraw`): `cases.getMyCase` (zero-arg, returns the caller's latest redacted case or null), `cases.submitResignation` (proposed last working day + notice-period days + optional reason → confirm dialog → `pending_approval`), `cases.withdraw` (pending-only, confirm dialog). When the latest case is terminal (rejected/withdrawn/cancelled/closed) a "Submit a new resignation" form reappears; otherwise a status card shows type/status/last-working-day/notice/reason plus a plain-language `explainStatus()` message and (pending-only) a Withdraw button. No HR tab strip for employees. Reusable `DialogShell`/`DialogHeader`/`DialogActions` keep each piece under Biome complexity 20. Browser-verified as employee: submit → Pending approval status card + Withdraw → Withdrawn + new form; 0 console errors; the test case was hard-deleted (cascade) via a throwaway script — zero seed pollution. Gates: build 0, web tsc 0 offboarding errors (26 baseline), lint 224/1/2 unchanged. Screenshots: `docs/reviews/phase-10d-cp5/`.
- **Phase 10D CP4**: ✅ Offboarding checklist actions — made the read-only CP3 case-detail page actionable with **no API changes** (every action wires to an existing 10C mutation; the API was built action-complete in 10C and exposed read-only through CP1–CP3). New `apps/web/src/features/offboarding/`: `use-invalidate-offboarding.ts` (one `invalidateQueries` predicate on `queryKey[0][0] === "offboarding"` — refetches the whole module after any mutation), `offboarding-note-prompt-dialog.tsx` (reusable `NotePromptDialog`, optional/required note + `danger` tone, used by skip/block/waive/reject/cancel/close to keep each action component flat under Biome complexity 20), `offboarding-task-actions.tsx` (Complete / Skip-note / Block-note-required), `offboarding-asset-actions.tsx` (Mark returned / Waive + `AddAssetDialog`), `offboarding-access-actions.tsx` (Mark revoked / Waive + `AddAccessDialog`, "disable outside Heimdallone" note), `offboarding-document-actions.tsx` (requested→Mark received, uploaded→Approve, Waive + `AddDocumentDialog`; file upload deferred to CP5), `offboarding-case-actions.tsx` (`CaseStatusActions` header: pending_approval→Approve/Reject, active→Move to clearance, in_clearance→Mark clearance complete, in_clearance|pending_settlement→Close case, non-terminal→Cancel; Close shows the "deactivates employee — only path that sets isActive=false" warning), `offboarding-interview-dialog.tsx` (`InterviewDialog` upsert: date/rating/reason/wentWell/couldImprove/wouldRehire[HR]/internalNotes[HR]/Keep-private-default-checked, prefills from the redacted read row). `cases/$id.tsx` sections gained an Actions column + Add/Request/Record buttons gated by `canManageOffboarding`; `SectionCard` gained a header `action` slot; extracted `toInterviewDefaults` to keep `InterviewSection` ≤ complexity 20. **RBAC is UX-only** — controls render only when `canManage`, the API re-checks every call. Browser-verified on the seeded in-clearance case as owner (Complete flipped To do→Done with counter 3→4 of 6, interview upsert logged to activity, Close confirm dialog showed deactivation warning and was cancelled — no side effect) and auditor (full case incl. settlement data, ZERO action buttons); the two test mutations were reverted via a throwaway scalpel script (zero seed pollution); 0 app console errors. Gates: build 0, web tsc 0 offboarding errors (26 pre-existing baseline), lint 224/1/2 unchanged. Screenshots: `docs/reviews/phase-10d-cp4/`.
- **Phase 10D CP3**: ✅ Offboarding cases UI — `apps/web/src/routes/app/offboarding/cases/{index,$id}.tsx` (flat `cases.tsx` → folder route, 4th route-shadow conversion), `apps/web/src/features/offboarding/offboarding-create-case-dialog.tsx` (HR-initiated case: employee picker via `hrCore.employees.list`, exit type, last working day, template, reason, internal note). Cases list: status filter pills (All / Pending approval / Active / In clearance / Pending settlement / Closed); columns employee (name resolved via merged active+inactive `hrCore.employees.list` map), exit type, status badge, last working day, per-case clearance progress (each `CaseRow` fires its own `tasks.list` → done/total); employees → no-access EmptyState (no queries). Case detail (read-only, the centerpiece): header + status badge; "What needs attention" panel derived from the section lists (no settlement 403 for managers); sections Summary / Clearance tasks / Asset returns / Access removal (with manual-tracking note) / Documents / Exit interview / Final settlement readiness (gated `canReadOffboardingSettlement`; shows "calculation handled later" disclaimer) / Activity timeline. Status label/tone helpers for task/asset/access/document added to `labels.ts`. Privacy is API-enforced (`redactCase` hides internalNote + involuntary exitReason; `interviewGetByCaseId` strips private/HR-only fields) — UI renders what each endpoint returns. **Client-typing note:** `cases.getById/list` return `Record<string,unknown>` (the API `redactCase` generic collapses the row type), so the UI casts to a `CaseView`; Drizzle date columns type as `Date` but serialize to strings, so `fmtDate` accepts `string | Date`. Browser-verified create→detail round-trip as owner (10 template tasks snapshotted, then hard-deleted the test case — zero seed pollution), filter narrowing, auditor read-only (no Create), employee no-access; 0 console errors. No mutation controls on detail yet (CP4). Screenshots: `docs/reviews/phase-10d-cp3/`.

- **Phase 10D CP2**: ✅ Offboarding templates UI — `apps/web/src/routes/app/offboarding/templates/{index,$id}.tsx` (converted the flat `templates.tsx` placeholder to a folder route — 3rd route-shadow conversion after recruitment/onboarding), `apps/web/src/features/offboarding/offboarding-template-form-dialog.tsx` (create/edit: name + exit-type select + description), label helpers `assigneeRoleLabel` / `dueOffsetLabel` added to `labels.ts`. List: name/description, exit type, Active|Archived status, task count (via `useQueries` on `templateTasks.listByTemplate`), category summary, updated date; `templates.list` returns a plain array (input `{includeInactive:true}` to show archived); employees get a no-access EmptyState with queries disabled (no 403 spam). Detail: summary (exit type / status / task counts / categories) + ordered tasks table with plain-language `dueOffsetLabel` ("N days before/on/after last working day") and human `assigneeRoleLabel`; "Task editing… coming in a later checkpoint" note. Create/edit/archive gated by `canManageOffboarding`; archive confirm copy "Archive this template? / Existing offboarding cases will not be changed." Extracted `ArchiveTemplateDialog` to keep the detail component under the cognitive-complexity ceiling (20). Browser-verified create→archive round-trip as owner (then hard-deleted the test row — zero seed pollution), auditor read-only (no Create/Edit/Archive), employee no-access; 0 console errors. Screenshots: `docs/reviews/phase-10d-cp2/`.

- **Phase 10D CP1**: ✅ Offboarding overview + tabs — `apps/web/src/features/offboarding/{offboarding-tabs,offboarding-placeholder,labels}.tsx`, `apps/web/src/routes/app/offboarding/{index,cases,templates,tasks,assets,access,my}.tsx`, `apps/web/src/styles/offboarding.css`. Removed the flat `routes/app/offboarding.tsx` placeholder that would shadow the folder routes (same fix as onboarding). Overview dashboard: 5 stat tiles (Active / Pending approval / In clearance / Pending settlement / Closed) sourced from `offboarding.cases.list` per-status count queries (`pageSize: 1`, read `.total`); "What needs attention" panel derives plain-language action items from the same counts (settlement item gated by `canReadOffboardingSettlement`); quick-links grid. RBAC (UX layer only — API is source of truth): HR/owner/admin/manager/auditor/payroll_admin see the dashboard via `canViewOffboarding`; employees fall through to a self-service "Coming later" placeholder (no tab strip). `cases`/`templates`/`tasks`/`assets`/`access` tabs render honest "Coming later" placeholders (ship in later CPs); `/my` is the employee self-service shell. Plain-language status labels in `labels.ts` (no raw enums as primary text). Placeholder card titles use a styled div (not a heading) to keep `h1→h2` order intact, matching `EmptyState`. Browser-verified across employee / owner / auditor with seeded data (0/1/1/1/1) and 0 console errors. **Gotcha hit:** the stale `bun run --hot` API server 404'd the new offboarding routes until restarted — see lessons-learned #76. Screenshots: `docs/reviews/phase-10d-cp1/`.

- **Phase 10C**: ✅ Offboarding oRPC API — `packages/api/src/routers/offboarding.ts` (9 router groups: templates / templateTasks / cases / tasks / assets / access / documents / interviews / activity / settlement). New `offboarding` AC resource (14 actions) in `permissions.ts`; reuses `resignation` resource for employee self-service. RBAC helpers `canManageOffboarding` / `canViewOffboarding` / `canReadOffboardingSettlement` mirrored in `role-helpers.ts` + `rbac.ts`. Two orthogonal authz layers: tenant scope (`verifyOBCase` on org_id + deletedAt) AND lateral manager→direct-report scope (`assertCaseVisibleToCaller`, applied to all 7 manager-reachable per-case reads — closed an IDOR found in review, commit `907ebaa`). `cases.create` / `submitResignation` are transactional (case + task snapshot + activity, with 23505 conflict → friendly "already has an active case"); `cases.close` is the ONLY procedure that sets `employeeProfile.isActive = false`. `redactCase()` hides `internalNote` from non-HR and the involuntary `exitReason` from the employee. audit:permissions = 62 pairs / 9 routers.

- **Phase 10B**: ✅ Offboarding DB schema + migration + seed — `packages/db/src/schema/offboarding.ts` (9 tables, 7 enums). Migration `0010_tricky_jimmy_woo.sql` applied. Seed: 3 templates / 23 template tasks / 4 cases across all key statuses (pending_approval, in_clearance, pending_settlement, closed) / 23 case tasks / 5 asset returns / 8 access revocations / 6 document requests / 2 exit interviews / 12 activity entries. Key constraints: partial unique `ob_case_employee_active_uq` (one active case per employee); partial unique `ob_exit_interview_case_uq` (one interview per case). `employeeProfile.isActive` NOT changed by schema/seed — set only by Phase 10C API close procedure. See [docs/implementation/offboarding-db-setup.md](../../implementation/offboarding-db-setup.md).

- **Phase 10A**: ✅ Offboarding spec — [offboarding-implementation-plan.md](../offboarding-implementation-plan.md). 9 entities (offboarding_case, _template, _template_task, _task, _asset_return, _access_revocation, _document_request, _exit_interview, _activity). Status lifecycle: pending_approval→approved→active→in_clearance→pending_settlement→closed (resignations); active→…→closed (HR-initiated). New `offboarding` AC resource (10 actions); reuses existing `resignation` resource. RBAC: owner/admin/hr_admin manage; manager scoped+task-complete; employee self-resign only; payroll_admin read_settlement; auditor read. Employee view restrictions: internalNote always hidden, isPrivate interview hidden, involuntary exitReason hidden. `employeeProfile.isActive = false` only on explicit case close. dueOffsetDays relative to lastWorkingDay (negative = before LWD). Asset/access as free text until Phase 12/14. Integration points: HR Core (isActive), Contracts (prompt but no auto-close), Payroll (readiness indicators), Assets Phase 12, IAM Phase 14.

- **Phase 9C**: ✅ Recruitment oRPC API — `packages/api/src/routers/recruitment.ts` (50 procedures across 10 entity groups: requisitions / jobs / candidates / applications / interviews / feedback / offers / offer approvals / documents / notes). New RBAC helpers `canManageRecruitment` and `canViewRecruitment` added to both `apps/web/src/lib/rbac.ts` and `packages/api/src/utils/role-helpers.ts`. Tenant-FK invariants enforced via 6 verification helpers (`verifyRequisition` / `verifyJobOpening` / `verifyCandidate` / `verifyApplication` / `verifyInterview` / `verifyOffer`). Every stage transition writes `application_stage_history`; terminal states (`hired` / `rejected` / `withdrawn`) cannot reverse without owner/admin + audit override. Candidate emails lowercased+trimmed; `deletedAt` rows excluded from default lists; interviewer FK arrays validated cross-tenant. Offer compensation fields stripped from output for non-payroll roles. ACL: reuses existing `posting / applicant / interview / offer / document` resources from `permissions.ts` — no new resources needed.
- **Phase 9B**: ✅ Recruitment DB schema + migration + seed — `packages/db/src/schema/recruitment.ts` (11 tables, 10 enums, relations), migration `0008_large_mindworm.sql`, `scripts/seed-recruitment.ts` (Atlas Shipping demo: 3 requisitions, 4 openings, 10 candidates, 10 applications across all 8 stages, 24 stage-history rows, 4 interviews, 3 feedback rows, 2 offers + approvals, 11 documents, 3 notes). 0 unique-constraint violations on candidate `(organizationId, email)`. Quality gates: check-types ✅, build ✅, ultracite 225 baseline. See [docs/implementation/recruitment-db-setup.md](../../implementation/recruitment-db-setup.md).
- **Phase 9A**: ✅ Recruitment + Onboarding spec finalization — see [recruitment-onboarding-implementation-plan.md](../recruitment-onboarding-implementation-plan.md). 18 proposed entities (12 recruitment + 7 onboarding — adjusted from initial 23 after consolidation: stages are enum + per-opening JSON, not a separate table; "hiring_manager" is per-opening employee FK, not a global role). Status lifecycles defined for requisitions, openings, applications, interviews, offers, onboarding, and onboarding tasks. UI routes + `RecruitmentTabs` + `OnboardingTabs` + new `KanbanBoard` / `TaskChecklist` primitives planned. RBAC matrix maps every capability to every role using the Phase 8J.2 normalized helpers. Candidate-to-employee conversion designed as one atomic API procedure with optional contract draft + onboarding handoff. Analytics shapes defined for both modules. 9B–9I sequence with effort estimates and parallelism notes. 10 open questions captured with recommendations.

### Product Standards Set in 8J.1
- **Module tabs are a product standard.** Recommended next implementations: Attendance, Leave, Employee Profile, Contracts. The reference implementation is `apps/web/src/features/payroll/payroll-tabs.tsx`.
- **Plain-language UX.** Never surface raw enum or audit codes as primary text. Codes may appear as small secondary text for support/debugging.
- **Manual payment confirmation only.** "Mark as paid" requires bank confirmation; exporting a bank file is not payment.

## Dependency Graph

```
HR Core (P0)
  │
  ├── Contracts (P0)
  │     │
  │     ├── Attendance (P1) ← Shifts from HR Core
  │     │     │
  │     │     ├── Biometric (P2) ← creates attendance events
  │     │     └── Geofencing (P2) ← validates check-in location
  │     │
  │     ├── Leave (P1) ← Holidays from HR Core
  │     │     │
  │     │     └── Compensatory Leave ← needs Attendance
  │     │
  │     └── Payroll (P1) ← needs Contracts + Attendance + Leave
  │           │
  │           └── Final Settlement ← needs Offboarding
  │
  ├── Recruitment (P2)
  │     └── Onboarding (P2) ← candidate-to-employee flow
  │
  ├── Offboarding (P2) ← needs HR Core, optionally Payroll + Assets
  │
  ├── Assets (P3)
  ├── Helpdesk (P3)
  ├── Projects (P3)
  └── Performance / PMS (P3)

Cross-cutting:
  Audit — implemented with HR Core, used by all modules
  Documents — implemented with HR Core, expanded per module
  Notifications — Phase 7+ (when approval flows exist)
  Automations — Phase 14 (after all triggers exist)
```

## What Can Be Parallelized

| Phase | Modules | Why parallel works |
|-------|---------|-------------------|
| 7 | Attendance + Leave | Both depend on HR Core but not on each other (compensatory leave deferred) |
| 9 | Recruitment + Onboarding | Onboarding depends on recruitment but can start simultaneously |
| 12 | Assets + Helpdesk + Projects | All independent, only need HR Core |

## What Must Wait

| Module | Must wait for | Reason |
|--------|--------------|--------|
| Payroll | Contracts (schema), Attendance (worked hours), Leave (deduction days) | Gross-to-net calculation needs all inputs |
| Biometric | Attendance | Creates attendance events |
| Geofencing | Attendance | Validates check-in |
| Compensatory Leave | Attendance | References attendance records |
| Leave encashment | Payroll | Creates payroll allowance |
| Final settlement | Payroll + Offboarding | Calculates remaining pay + deductions |
| Automations | All core modules | Needs triggers from all entity types |

## First 3 Milestones After HR Core

### Milestone 1: "Employees Live" (Phase 5B)
- Employee list with real data (DataTable, filters, search)
- Employee profiles with all tabs (personal, work, bank, documents, activity)
- Employee creation wizard
- Organization settings (departments, positions, shifts)
- Holiday management
- Audit event logging
- **Value**: HR can manage employee records, replacing spreadsheets

### Milestone 2: "Contracts + Time Tracking" (Phase 6 + 7)
- Employment contracts with status lifecycle
- Manual check-in/out with daily attendance records
- Overtime calculation and approval
- Leave type configuration with balances
- Leave request and approval workflow
- Team calendar
- **Value**: HR can track attendance, manage time-off, and prepare for payroll

### Milestone 3: "Payroll Running" (Phase 8)
- Pay item configuration (allowances, deductions)
- Payslip generation with gross-to-net
- Pay run wizard with preview
- Employee payslip portal
- Loans and reimbursements
- **Value**: Organization can run payroll through Heimdallone

## Highest-Risk Modules

| Module | Risk Level | Why |
|--------|-----------|-----|
| Payroll | Very High | Complex calculations, multi-country tax, money precision, high-stakes errors |
| Attendance | High | Edge cases (midnight, missing checkout, timezone), device integration |
| Leave | High | Balance calculations, carry-forward, concurrent approval race conditions |
| Biometric | High | Physical device connectivity, multiple protocols, sync reliability |
| Contracts | Medium | One-active constraint, mid-period changes, salary sync |
| Recruitment | Medium | Kanban drag-drop UX, candidate-to-employee conversion |
| Performance | Medium | 360 feedback from multiple sources, review fatigue |

## Staff Training / Adoption Risks

| Module | Adoption Risk | Mitigation |
|--------|--------------|------------|
| Payroll | High — payroll clerks used to Excel | Step-by-step wizard, preview before generate, "why blocked" panels |
| Attendance | Medium — field workers may struggle with check-in | Big check-in button on dashboard, mobile-friendly, grace time |
| Leave | Low — employees already request leave | Intuitive calendar, balance cards, clear status badges |
| Performance | Medium — review fatigue, unfamiliarity with OKRs | One-question-at-a-time review, simple goal progress sliders |
| HR Core | Low — HR staff are primary users | Setup checklist, guided wizard, smart defaults |
| Recruitment | Low — recruiters understand pipeline concept | Familiar kanban UX |
| Assets | Low — straightforward inventory | Simple table with assign/return actions |
| Helpdesk | Low — employees understand ticketing | "What do you need help with?" simple form |

## Shared Primitives Still Needed

| Primitive | Needed by Phase | Status |
|-----------|----------------|--------|
| DataTable | 5B (HR Core) | ✅ Built (Phase 4F) |
| StatusBadge | 5B | ✅ Built |
| EmptyState | 5B | ✅ Built |
| EntitySheet | 5B | ✅ Built |
| ConfirmDialog | 5B | ✅ Built |
| PageHeader | 5B | ✅ Built |
| ActionMenu | 5B | ✅ Built |
| FilterBar | 5B stretch / 7 | ⬜ Not yet |
| SavedViewTabs | 5B stretch / 7 | ⬜ Not yet |
| BulkActionToolbar | 5B stretch / 7 | ⬜ Not yet |
| WizardForm | 5B (employee create) | ⬜ Not yet |
| FormSection | 5B | ⬜ Not yet |
| FieldHelp | 5B | ⬜ Not yet |
| ViewSwitcher | 7 (attendance calendar) | ⬜ Not yet |
| AuditTimeline | 5B (employee activity) | ⬜ Not yet |
| ApprovalQueue | 7 (leave/attendance approvals) | ⬜ Not yet |
| KanbanBoard | 9 (recruitment pipeline) | ✅ Done (Phase 9D) |

## Modules Needing More Research

| Module | What's needed |
|--------|--------------|
| Payroll | GY 2026 ✅ implemented. BB 2026 + TT 2026 researched (rates documented in payroll-implementation-plan.md) — need official verification before production. TT 2027 NIS rate change (19.2%) needs separate module. JM PAYE still unresearched. Bank file formats for local banks still needed. |
| Biometric | ZKTeco SDK/protocol documentation. Anviz cloud API docs. Test device availability. |
| Geofencing | GPS accuracy requirements for Caribbean field operations. Mobile app capabilities. |
| Performance | OKR framework preferences for Caribbean organizations. Appraisal cycle norms. |
